# 项目交接记录

本文件记录跨 Cursor / Codex 切换时的待办与完成状态。每次完成一项工作后在此更新，便于换工具后继续。

来源：2026-08-05 Codex 会话中用户标记「稍后再做」的 UI 改进（Codex 已记录但未写代码）。

## 2026-08-20 materials 汇总校验与 Server 写入确认布局

- 已完成：material 解析继续以 `Color Table` 作为 Panel/封边正式来源，但新增校验 `Total Qty:` 与 Color Table 的 3/4 Panel、1/4 Panel、封边合计；不一致时阻止材料事实替换并提示用户手工处理。
- 已完成：有 Panel 或封边数量的明细行必须填写 Color；空模板行不触发提醒。当前 CS004 现场文件只读解析确认 `vanity` 行因 Color 为空而阻断。
- 已完成：Server 订单材料确认弹窗将“稍后处理”和“确认写入订单材料”移动到同一底部操作组；完成写入后保留“关闭”状态。
- 已新增回归测试：覆盖空 Color、Total Qty/Color Table 合计一致与不一致、公式显示值和多颜色表。
- 已验证：完整发布门禁 225 项 Python 测试、macOS UI 回归、PP0067 workbook E2E、差异检查通过；重新构建成功。安装脚本因 Codex 后台会话无法访问 Apple Development 签名身份，尚未完成正式安装版验证；没有修改 Server 文件或外部库存数据。

## 2026-08-20 修复 Server 房间分配绕过材料校验并回退 CS004-vanity

- 已修复：Server 预览在使用 `parse_material_room_rows()` 做房间/工厂单归属时，先强制执行订单级 `parse_order_materials()`；Color 为空和 `Total Qty:`/Color Table 合计不一致现在会阻止预览与后续写入。
- 已新增回归测试：覆盖单订单房间明细路径中 Color 为空时不能生成 Server 材料预览。
- 已回退本地中央数据库中的 CS004-vanity 状态：删除工厂单 `F2608190228`、两条 vanity Server 报表基线、对应 batch evidence、生产批次和相关当前事实；保留 CS004-Kitchen、CS004 订单和历史 `sync_changes` 审计记录。未修改 Server 原文件。
- 回退前备份：`data/database-backups/workflow-20260820-1010-pre-cs004-vanity-rollback.sqlite3`；回退后 `workflow.sqlite3` 完整性检查为 `ok`。
- 已验证：226 项 Python 发布测试、macOS UI 回归、PP0067 workbook E2E、差异检查通过；最新 App 构建成功。正式安装仍受 Codex 后台会话无法访问 Apple Development 签名身份限制，需从普通 macOS Terminal/Aqua 会话安装后再做安装版验证。

## 2026-08-20 Server 扫描误解析材料表修复

- 已确认原因：出库审计记录的 `traveler_path` 在订单级/房间级材料单场景可能保存 `materials.xlsx`；Server 扫描判断已出库订单时，旧逻辑只按 `.xlsx` 后缀调用 Traveler 解析器，导致 `Sheet1` 材料表触发“Traveler 缺少必要工作表”。
- 已修复：只有文件名符合 `Work Order Traveler(...)` 时才调用 Traveler 解析器；不兼容的材料源文件保留已确认出库状态，不再推断为“需要更新”；历史 Traveler 格式解析错误也不会阻断 Server 扫描。
- 已新增回归测试：覆盖材料表被记录为 `traveler_path` 时的 Server 扫描和已出库订单筛选。
- 已验证：完整发布门禁 223 项 Python 测试、macOS UI 回归、PP0067 workbook E2E、差异检查通过；重新构建、Apple Development 签名安装和安装版 `scan-server` 现场验证通过，当前问题列表为空。本次未修改 Server 文件或外部库存数据。

## 2026-08-19 设置页操作日志大小与近三天清理

- 已实现：设置页“操作日志”卡片显示当前日志文件大小，并新增“清理至近三天”按钮；卡片继续复用现有设置卡片样式。
- 已实现：清理按本机日历保留今天及前两个自然日；兼容带 UTC/时区偏移的 ISO8601 时间戳，清理使用原子替换，并在日志格式异常或并发变化时停止，不覆盖原文件。
- 已验证：日志清理边界回归测试、Python 222 项发布测试、macOS UI 回归、PP0067 workbook E2E、构建、Apple Development 签名安装和安装版启动检查通过。
- 现场限制：当前 macOS 会话未授予 System Events 辅助功能权限，无法自动点击安装版“设置”标签；设置页控件已完成源码编译与回归验证，实际点击查看和清理仍需用户在安装版中手工确认。

## 2026-08-19 计划安装日期允许暂不填写安装人

- 已修复：保存计划安装日期或实际安装日期时，安装人/安装小组可以暂时为空，后续可在同一编辑区补填；日期格式和重复日期校验保持不变。
- 已同步修改 SwiftUI 前端校验、Python 数据层校验和订单索引回归测试；没有修改已有订单安装数据，也没有执行库存写入。
- 已验证：221 项 Python 发布测试、macOS UI 回归、PP0067 workbook E2E、重新构建、Apple Development 签名安装和安装版启动/退出检查通过。

## 2026-08-19 已出库订单清理陈旧 Server 待处理问题

- 已确认原因：自动扫描/处理入口会跳过全部工厂单已出库的标准订单文件夹，但此前产生的 `active_issues` 材料校验问题没有随出库完成自动关闭。
- 已修复：订单完成全部出库后，自动将其 Server 文件夹下的 `material_validation` 问题标记为 `resolved`；`sync_changes` 历史记录保留不变。
- 已回写现场数据：PP0068 的旧材料校验提醒已解决；当前仍开放的相关问题为 PP0072 材料校验和 PP0067 五金清单问题。
- 已验证：新增回归用例；完整发布测试 219 项、macOS UI 回归、PP0067 workbook E2E、构建、Apple Development 签名安装和安装版启动检查均通过。

## 2026-08-19 出库预览、失败重试与 PP0068 出库完成

- 已实施出库预览排序：`Plywood → Panel → 封边条 → 五金`；Plywood 按 `18mm=柜体板、14.5mm=抽屉板、5.4mm=背板` 排列，未知厚度放在最后，不猜测业务类型。
- 已实施失败后原界面重试：保留预检数据和当前选择，失败后按钮改为“重试出库”，重试不要求重新选择工厂单。
- 已实施精斗云历史列表等待修复：历史列表必须等待查询控件实际出现；未完成加载时自动重新打开并重试，不把新增表单误认为历史列表。
- 已修复多单据超时的部分同步：前一张单据已成功而后一张超时/失败时，先把已返回的成功结果写入本地，再提示查询历史后重试。
- 已修复库存同步审计文件落盘：中心 SQLite 更新时同步写入 `inventory-outbound-records.json`；Server/SMB 备份的 macOS 属性复制失败时降级到本地 `data/database-backups/Inventory Sync Records`，不能阻断已确认的库存单同步。
- 已修复对账显示：同一工厂单存在订单级材料单和工厂级五金单时，优先使用工厂级记录，并显示全部已确认单据号。
- PP0068 已核验并同步：材料单 `QTCK20260820004`、五金单 `QTCK20260820005`；精斗云历史查询显示两张单据均存在，`F2607290218 / PP0068-Kitchen` 当前为“已出库”，出库单号为 `QTCK20260820004、QTCK20260820005`。
- 已验证：发布测试 218 项、macOS UI 回归、PP0067 workbook E2E、构建、Apple Development 签名安装和安装版启动检查均通过。

## 待后续实施：出库预览材料分类与排序

- 用户要求：出库预览固定按 `Plywood → Panel → 封边条 → 五金` 显示；Plywood 内按“柜体板 → 抽屉板 → 背板”显示。
- 当前判断：订单数据库已有 `material_type`（`plywood`、`panel`、`edge`）和 `hardware_items`，Plywood 子分类沿用 18mm=柜体板、14.5mm=抽屉板、5.4mm=背板的现有业务规则；未知厚度不得猜测，应显示未分类或提示。
- 出库失败后重试：失败应保留在当前“本次出库预览”界面，提供明确的“重试”入口；重试应复用当前选择和预检数据，不能要求用户退出后重新选择工厂单。
- 库存系统自动化：改进 `tools/jdy_inventory.mjs` 的历史列表等待逻辑，明确等待 `initOiList` 和查询控件加载完成；必要时重新打开页面并重试，不把精斗云停留在新增表单视为可查询状态。
- 本项已恢复执行并完成；具体实施结果见上方“2026-08-19 出库预览、失败重试与 PP0068 出库完成”。

## 2026-08-19 PP0067 出库单 QTCK20260820003 本地同步

- 用户确认 PP0067 已出库，单据编号为 `QTCK20260820003`。
- 已核对：PP0067 只有一个活动且已确认的工厂单 `F2607280216`；同步前无该单据记录，工厂单状态为“未出库”。
- 已备份并写入中央 `workflow.sqlite3`：新增一条 `outbound_documents` 和一条 `outbound_document_factories` 关联，来源标记为 `user_confirmed`；未执行外部库存系统查询或写入。
- 已验证：数据库完整性检查为 `ok`；`F2607280216` 为“已出库 / QTCK20260820003”，PP0067 出库进度为 `1 / 1`，订单阶段为“已出货”。

## 2026-08-19 PP0035 全部出库后的订单阶段修复

- 已确认：中央 `workflow.sqlite3` 完整性检查为 `ok`；PP0035 当前 20 个活动工厂单全部为 `outbound_status=已出库`，出库进度为 `20 / 20`，原订单 `stage` 却仍为“已拆单待优化”。
- 已确认原因：订单汇总先判断 `optimized < expected`，再判断 `shipped == expected`；PP0035 的优化证据为 `0 / 20`，因此全量出库状态被较早的未优化分支遮蔽。
- 已修复：将“全部出库”设为终止阶段优先级，PP0035 当前数据已回写为 `stage=已出货`；20 条工厂单的出库状态和单据号未修改。
- 已备份：修复前数据库归档至 `data/migration-archives/20260819-pp0035-shipped-stage-pre-fix/workflow.sqlite3`。
- 已验证：新增回归用例覆盖“优化证据缺失但全部出库”，完整发布测试 217 项、macOS UI 回归、PP0067 workbook E2E、构建、Apple Development 签名安装均通过。安装版启动时发现已有 PP0067 真实出库任务正在运行，未中止该不可取消任务，因此 PP0035 安装版窗口直读核验被该任务暂时占用；中央数据库和安装包校验已完成。

## 2026-08-19 AIMES 精确核验结果未写回修复

- 已确认原因：`F2606170156` 被加入 AIMES 精确核验请求，但 AIMES 返回的 `verify_rows` 与最近页面 `rows` 分开；同步流程只持久化最近页面 rows，精确结果只用于判断是否删除，因此已有的 `server_report` 来源没有升级为 `AIMES`。
- 已修复：AIMES 同步在分区、写入数据库和后续订单候选处理前，将最近页面 rows 与精确核验 rows 按工厂单号合并；精确核验仍不扩大批量抓取范围。
- 已修复：`factory_orders` 作为完整持久化索引保留每次获取到的有效工厂单；最近 50 条只是在线批量窗口。Server 匹配先复用数据库中已有工厂单身份，只有数据库没有该工厂单号且现有报表无法确认归属时才调用 AIMES 精确查询；AIMES 映射缓存也按工厂单号累积合并，不再被 50 条结果截断。
- 已验证：215 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动/退出检查通过。现场数据库确认 `F2606170156 -> PP0035 -> PP0035-ROOM 5`、来源 `AIMES`，PP0035 活动工厂单 20 条；本轮再次在线获取因 AIMES 登录未成功而未刷新，但未改动已有数据库事实。

## 2026-08-19 PP0035-ROOM 5 订单归属展示修复

- 已确认原因：中央 SQLite 已有 `F2606170156 -> PP0035 -> PP0035-ROOM 5`，归属状态为“已确认”；订单汇总代码对普通订单只保留 `name_source=AIMES` 且销售单名称匹配的工厂单，导致来源为 `server_report` 的 ROOM 5 被汇总阶段过滤，UI 未显示。
- 已修复：普通订单继续优先使用 AIMES 销售单名称匹配；对数据库中已明确确认归属到该订单的 Server 报表或人工工厂单，按已保存 `order_id` 纳入订单工厂单列表。
- 数据边界：本次不修改生产订单/工厂单事实，只修正汇总展示规则；现场数据库中的 ROOM 5 记录保持不变。
- 已验证：213 项 Python 发布测试、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装及安装版启动/退出检查通过；实时汇总已包含 PP0035-ROOM 5。

## 2026-08-19 空 Color Table 自动汇总

- 已实现：当 material 上方明细行包含颜色和板材/封边数量、下方 `Color Table` 为空或缺少颜色时，只在下方补充颜色与 `SUMIF` 合计公式；不修改上方明细行，也不修改 `Total Qty:` 行。
- 单颜色旧格式现在也会补充 Color Table；解析器保留旧格式的 `Total Qty:` 权威值，并在其为空时回退到明细行汇总。
- 已验证：206 项 Python 回归、macOS UI 回归、PP0067 workbook 端到端、构建、Apple Development 签名安装和安装版启动/退出检查均通过。未执行真实 Server 同步或库存写入。

## 2026-08-19 消息记录悬停前台判断

- 已完成：消息记录悬停详情在真正弹出前检查 App 是否处于前台，以及消息所在主窗口是否仍是当前 Key/Main Window；切到其他 App、打开其他主界面或窗口后，不再显示悬停面板。
- 已调整：鼠标离开消息行或悬停面板后的关闭宽限期统一改为 0.8 秒；宽限期内重新移回时继续保留面板。

## 2026-08-19 PP0072 订单类型与阶段修复

- 已确认原因：PP0072 的标准 Server 文件夹和 AIMES 工厂单归属均正常，但订单索引中的 `order_type` 曾被旧流程写成 `temporary`；后续同步传入空订单类型时，`upsert_order` 保留了旧值。汇总逻辑因此固定显示“临时任务 / 待人工处理”。
- 已修复：处理标准或混单 Server 文件夹时，按订单号重新写入 `owned` 或 `cutToSize`，不再保留陈旧的 `temporary` 类型。PP0072 已重新处理并恢复为“自有订单 / 已优化 / 正常”，工厂单 `5 / 5`。
- 已验证：208 项发布测试、构建、Apple Development 签名安装和安装版界面检查通过；本次只更新本地订单索引，没有执行外部库存出库。

## 2026-08-19 订单详情 Panel 同色厚度排序

- 已完成：订单详情 Panel 按颜色归组；同色 Panel 内固定先显示 `19.1mm` 正常板，再显示 `8mm` 或 `9mm` 背板，其他厚度排在最后；大小写、首尾空格和重音差异不会拆分同色分组。
- 已验证：208 项发布测试、构建、Apple Development 签名安装通过；安装版订单中心已核验 PP0072 的 Panel 颜色与工厂单列表正常显示。

## 2026-08-19 出库单号显示与订单文件映射入口修正

- 已完成：工厂单出库状态在有单据编号时显示为“已出库 · 单据编号”；没有单据编号或尚未出库时不显示历史编号。
- 已完成：待处理中心的“处理订单文件映射”按钮现在会关闭待处理中心并弹出映射工作台，从报错信息提取未映射材料名称（例如 `LED`），支持直接搜索并保存库存商品映射；保存后继续按原文件路径重新读取 Server 文件夹。
- 已完成：未映射材料在工作台中可选择“处理映射”或“忽略”；忽略会弹出确认窗口，明确显示材料名称、全局忽略影响和忽略原因，保存后按原文件路径重新读取 Server 文件夹。
- 已完成：重新读取时统一从订单根目录开始，并在读取完成后显式执行 `list-index` 刷新主界面订单列表；同时清理已经解决的“未完成商品 SKU 处理”旧状态。忽略 `LED` 后，PP0072 当前为 `正常`、阶段 `待人工处理`、工厂单 `5 / 5`，待处理中心不再保留 PP0072 的映射问题。
- 已验证：完整发布测试 208 项、macOS UI 回归、构建、Apple Development 签名安装均通过；安装版已实际核验 PP0072 主列表显示 `临时任务 待人工处理 5 5 / 5` 和 `正常 未出库`。本次只刷新本地订单数据库，没有创建外部库存出库单。

## 2026-08-19 多工厂单共用出库单的状态恢复修正

- 用户确认 PP0046 的 `QTCK20260729007` 同时覆盖 `F2607220203`、`F2607220204`、`F2607220205`；旧做法把三个工厂单号拼在 `outbound_documents.factory_order` 中，自动 `sync-index` 无法匹配并将三个状态覆盖为“未出库”。
- 已实施：新增 `outbound_document_factories` 关联表；历史组合记录自动迁移为三条精确工厂单关联；出库记录加载、对账、库存写回和订单详情读取均兼容关联模型；订单级单据仍只保存一条主记录。
- 已验证：关联迁移与多工厂对账定向测试通过；数据库现场迁移后 PP0046 保持“已出货 / 3 / 3”。完整发布测试 205 项、macOS UI 回归、PP0067 workbook 端到端测试、构建、Apple Development 签名安装均通过；安装版启动后的 `list-index`、`sync-aimes`、`scan-server`、`sync-index` 全部完成，自动扫描后 PP0046 仍为 3/3 已出库。未执行外部库存系统复核。

## 2026-08-19 AIMES 阶段耗时与总耗时口径修正

- 已确认原因：AIMES 子进程曾把“获取 AIMES 数据成功，总计用时”同时放入阶段数组；精确核验工厂单存在性实际执行但没有独立阶段；SwiftUI 还保留本地计时与后端权威计时两套来源。
- 已完成：移除聚合总耗时阶段，补充“精确核验工厂单存在性”，将后端准备/收尾未分解耗时作为独立补齐阶段，并让成功结果只使用后端返回的总耗时；兼容过滤旧格式聚合阶段。
- 已完成：新增 Python 与 macOS UI 回归断言，完整发布测试、构建、Apple Development 签名安装、安装版启动/后台初始化/退出检查均通过。
- 现场限制：本次未重新触发真实 AIMES 在线获取，只验证了已有操作日志、源码契约、测试 fixture 和安装版启动；真实 AIMES 新一轮显示仍需用户下一次点击“获取 AIMES”后确认。

## 2026-08-18 订单备注与安装安排

- 已实现订单级备注，以及计划安装日期、实际安装日期的独立明细保存；安装人/安装小组可以暂时为空，后续再补填，日期可不连续且两类日期均可为空。
- 订单中心列表在订单号下显示备注和安装日期摘要；订单详情增加“订单备注与安装安排”编辑区，可添加/删除日期、填写安装人并保存。
- 开始安装日、结束安装日和安装天数由日期列表自动计算；例如 7 月 8 日、7 月 11 日显示为开始 7/8、结束 7/11、共 2 天。
- 订单同步不会覆盖用户备注和安装安排；重复日期或无效日期时拒绝保存，安装人允许暂时为空。
- 已完成：202 项 Python 发布测试、macOS UI 回归测试、构建、Apple Development 签名安装和安装版启动/退出检查。真实订单现场填写安装数据尚未执行。

## 2026-08-18 已出货订单操作限制

- 用户确认：订单状态为“已出货”时，只能计算成本，不能查询库存、设置出库范围或创建出库。
- “部分出货”不属于完成状态；如果仍有未出库工厂单，继续保留对应库存和出库流程。

## 2026-08-17 成本汇总表列宽调整（待后续实施）

- 用户要求：成本汇总表第一列“材料类型”宽度减小，后两列“成本”“状态”宽度增大，使三列内容显示更整齐、协调。
- 参考范围：用户提供的成本汇总表截图；只调整列宽和相关对齐/留白，不改变材料名称、成本金额、状态文字或业务逻辑。
- 本次状态：仅记录需求，暂不修改代码、测试、构建或安装 App。

## 2026-08-16 AIMES 映射与耗时口径修正

- AIMES 销售单名称格式异常改为一次性获取警告：不写入业务映射、不进入待处理中心，需回 AIMES 修改原始数据。
- AIMES 成功耗时由后台统一返回，覆盖浏览器启动、重试和数据库写入；批次号继续沿用 Server 报表文件逻辑。

## 2026-08-16 订单中心列表与悬停菜单修正

- 订单中心悬停消息保留消息行作为稳定的生命周期锚点，同时恢复按鼠标位置决定显示在左侧或右侧；滚轮滚动或轻微鼠标移动不会立即关闭详情，仅当鼠标离开消息行和详情面板并超过短暂宽限时间后关闭。
- 订单中心默认“未完成订单”模式只显示工作流程尚未完成的订单；选择上方“已出货”状态仍可查看历史已完成订单，不改变数据库事实。

## 待办总览

| # | 内容 | 状态 | 完成日期 |
|---|---|---|---|
| 1 | 库存页：「更新商品资料」「刷新列表」移到右侧，与操作记录区对齐 | 已完成 | 2026-08-06 |
| 2 | 库存页：Traveler 文件名悬停横向滚动 | 已完成 | 2026-08-06 |
| 3 | 整体布局：左侧主导航改为顶部横向导航 | 已完成 | 2026-08-06 |
| 4 | 增大预览区域 + 提高 App 默认窗口尺寸 | 已完成 | 2026-08-06 |
| 5 | 浏览器自动化后台运行，不抢占用户窗口或键盘焦点 | 已完成 | 2026-08-06 |
| 6 | 服务器离线时生成并切换到本机测试订单目录 | 已完成 | 2026-08-06 |
| 7 | 生产文件页、出库页：移除“操作记录”标题，下方组件整体上移 | 已完成 | 2026-08-07 |
| 8 | 默认窗口改为参考图大小，并压缩首屏布局以避免纵向滚动 | 已完成 | 2026-08-07 |
| 9 | 生产文件页：去除“本机测试订单”界面及“服务器 / 本机测试”切换 | 已完成 | 2026-08-07 |
| 10 | Report 无法匹配工厂单名称时，通过 AIMES 查询缺失的工厂单号与名称 | 已完成 | 2026-08-07 |
| 11 | 将 AIMES 获取的工厂单号/名称对应关系保存在项目数据文件，后续优先读本地记录 | 已完成 | 2026-08-07 |
| 12 | 切换生产文件或出库页左侧不同文件时，清空右侧操作记录，只显示当前文件的记录 | 已完成 | 2026-08-07 |
| 13 | 合并全部页面头部：移除下方页面标题区，将其图标、标题、副标题移到上方导航区左侧，替换原应用名称内容 | 已完成 | 2026-08-07 |
| 14 | 重新设计待办页图标：明确体现任务清单/待办事项，并与其他页面图标保持一致风格 | 已完成 | 2026-08-07 |
| 15 | 设置页：缩窄“文件位置”卡片并移动到“运行范围”下方，确保默认窗口无需滚动即可完整显示 | 待办 | — |
| 16 | 设置页：系统账户中的两个保存按钮统一排列并垂直对齐 | 待办 | — |
| 17 | 出库页：删除“模拟写入”按钮，剩余四个按钮单行排列；排查并修复“设置映射”按钮无响应 | 待办 | — |
| 18 | 全页面状态区优化：取消页面顶部独立消息显示，消息与操作记录统一放入下方消息记录框；同时缩小按钮间距 | 已完成 | 2026-08-14 |
| 19 | 设置页增加/完善 AIMES 用户名密码保存，密码写入 macOS 钥匙串并在查询前校验配置 | 已确认待修复 | — |
| 20 | 设置页布局：将“文件位置”缩窄并移动到左侧红框位置，与“运行范围”“系统账户”三块整齐排列；系统账户两个保存按钮与输入框左对齐 | 待办 | — |
| 21 | 生产文件页右侧三个按钮与下方内容区域右对齐，并缩小按钮间距；状态消息限制为单行精简显示 | 已完成 | 2026-08-07 |
| 22 | 修复 AIMES Playwright 找不到 Chromium 浏览器的问题，优先使用本机 Google Chrome | 已完成 | 2026-08-07 |
| 23 | 点击左侧订单文件夹后，右侧详情区左边缘被裁切；改为分栏宽度自适应并保持内容左对齐 | 已完成 | 2026-08-07 |
| 31 | 新增订单看板：订单摘要、展开工厂单、订单详情与现有生产/出库入口整合 | 原型设计中 | — |
| 32 | 订单中心默认启动窗口改为用户本次 App 截图所示大小；实施时以 AppShot 的实际窗口 frame/point 尺寸校准 | 待办（已记录） | — |
| 54 | 当前问题入口：有未处理问题时显示警示图标，图标风格与右侧信息图标一致；无未处理问题时隐藏警示图标；点击后继续显示详情 | 已完成 | 2026-08-11 |
| 55 | 设置页整体布局：参考用户截图重新整理运行范围、系统账户、库存商品资料与材料映射等卡片的排列、宽度、间距和对齐，使页面整体整齐一致 | 已完成 | 2026-08-11 |
| 53 | 库存页：更新商品资料成功后出现弹出提示；需后续确认提示的触发时机、内容和“更新商品资料”按钮显示是否重复 | 已完成 | 2026-08-12 |
| 76 | 订单中心消息队列：消息没有按时间顺序显示，需后续定位排序来源并修复 | 待诊断修复 | — |
| 78 | 订单中心悬停菜单：移除操作按钮；所有操作统一从待处理中心进入 | 已完成 | 2026-08-12 |
| 79 | 设置页：压缩系统账户卡片高度并增加操作日志查看弹窗，以时间和操作内容展示日志 | 已完成 | 2026-08-13 |
| 59 | 订单中心多选工厂单后进入上下文出库预览；移除该入口出库窗口左侧 Traveler 列表；数量为 0 的 Traveler 项目不显示 | 已完成 | 2026-08-14 |
| 90 | 出库预览页：缩小窗口宽高以完整显示预览与操作信息；移除右上“更新商品资料”“刷新列表”、底部“后台模拟填写”；将“确认写入库存系统”改名为“确认出库” | 已完成 | 2026-08-14 |
| 91 | 订单中心工厂单表格：放大第一列前面的单选框，并在该列内水平居中 | 已完成 | 2026-08-14 |
| 92 | 出库复用已有库存系统 Chrome 后，仓库菜单固定 ID 定位超时；改为复用已有其他出库单列表，并兼容悬停/点击及可见菜单入口 | 已完成 | 2026-08-14 |
| 93 | 修复订单/工厂单状态持久化：出库成功回写标准工厂单 SQLite 状态；启动和索引读取时恢复库存出库状态；局部工厂单更新不覆盖已拆单、已优化、已出库等既有状态 | 已完成 | 2026-08-14 |
| 94 | 订单中心接入订单/工厂单材料详情：在订单详情和工厂单详情中展示板材、封边和五金明细 | 部分完成（订单详情已显示板材/封边及按工厂单分组的五金；工厂单级材料关联仍待补充） | 2026-08-15 |
| 95 | 订单中心改用数据库事实流：取消 Traveler 生成/校验前置门禁；选中订单后才按需生成 Traveler，并支持用户另存和打印 | 已完成（订单详情、库存查询、商品映射和出库均使用数据库事实；Traveler 仅为按需导出） | 2026-08-15 |
| 96 | 使用昨天拷贝的 Server、AIMES 本地缓存和库存出库记录补齐中央 SQLite 订单事实 | 已完成（13 个标准订单目录已同步；临时/混单目录保留待人工处理） | 2026-08-15 |
| 97 | 订单详情布局与忽略五金治理：移除“订单详情”标题；内容区加宽并保持四列卡片；仅五金区域滚动；隐藏五金单位；忽略五金不写库、不出库并清理历史残留 | 已完成（当前忽略清单为空，实际数据库删除 0 条） | 2026-08-15 |
| 98 | 订单详情内容整体水平居中；核查无商品编号五金是否为忽略残留 | 已完成（当前 `ignored` 清单为空；PP0047 的 Square star basin 是 active AICNC 五金事实，不自动按缺少商品编号删除） | 2026-08-15 |
| 99 | 订单中心默认隐藏工作流程已完成的订单；悬停消息改为稳定锚点并支持滚轮查看 | 已完成 | 2026-08-16 |
| 109 | 在 Server 主机部署开机常驻扫描服务，由 App 读取扫描快照 | 后续大版本候选（已记录） | — |
| 110 | Server 扫描详情显示快速检查、复用、深度扫描数量和总用时 | 已完成 | 2026-08-17 |

## 已完成（Codex 同日，Git 未提交）

- Picking List 空白模板保留（CS004）
- 库存页操作按钮自适应网格布局

## 第 31 项：订单看板原型与后续实现边界

- 已确认 App 核心信息对象为订单，订单下展示 AIMES 工厂单、优化证据、材料、库存和出库状态。
- 客户变更时在原订单号下新增不同号、不同名称的独立工厂单，不建立替代关系。
- 订单已出货后新增未出库工厂单，订单状态回退为部分出货，并按最新工厂单集合重新计算出货进度。
- 当前版本只设计 Dashboard、工厂单展开、订单详情和现有生产文件/出库入口；WeCom 与 CNC 集成留待下一版本。
- 原型和后续实现保持当前 App 默认窗口大小，不修改窗口尺寸常量。
- 后续实现优先复用现有 SwiftUI 组件、订单预览模型、库存比较和出库流程，不复制业务逻辑。
- 详细界面与操作规格见 `docs/order-dashboard-prototype-spec.md`。

## 第 32 项：订单中心默认窗口尺寸（待后续执行）

- 用户希望 App 打开时默认就是本次截图所示的窗口大小。
- 当前只记录需求，不修改 `defaultSize`、最小窗口或布局常量。
- 执行时先从 macOS AppShot/窗口 frame 读取实际 point 尺寸，再更新默认窗口配置；不能直接按截图像素估算，避免 Retina 缩放导致尺寸偏差。
- 修改后需重新运行 UI 回归测试、构建、正式安装，并验证首次启动窗口尺寸。

## 第 94 项：订单中心接入订单/工厂单材料详情（待实施）

- 用户要求：订单中心需要能够查看订单材料详情，以及具体工厂单对应的材料/五金详情。
- 当前缺口：订单中心展开行目前只显示订单摘要、Panel 颜色、工厂单号/名称、拆单、优化和出库状态；点击工厂单行只是选择工厂单，不会打开材料详情。
- 已确认现状：订单详情后台命令已经读取数据库中的 `materials` 和 `hardware`，但 `OrderDashboardDetailCard` 只展示 Panel 颜色摘要，没有把完整板材、封边和五金明细接入订单中心界面。
- 后续范围：复用现有订单预览模型和材料展示组件，在订单详情中展示板材/封边明细，在工厂单详情中展示对应工厂单的五金及可关联材料；明确区分订单级材料与工厂单级材料，避免把订单总量错误分配到每个工厂单。
- 入口方向：从订单中心订单行“查看详情”展开后，增加材料详情入口或详情区域；点击具体工厂单后进入该工厂单上下文，不能仅作为出库多选控件。
- 本次状态：订单详情页已接入数据库材料展示；工厂单级五金已按工厂单分组显示，工厂单级板材/封边关联仍待补充。已完成测试、重建、正式安装和已安装 App 界面验收。
- 待验证：使用本地订单缓存覆盖有板材、封边、五金、空明细和多工厂单的订单；确认订单级与工厂单级数量、颜色、规格、单位显示正确，并完成正式安装后的 UI 验收。

## 第 95 项：订单中心数据库事实流与 Traveler 按需导出（待实施）

- 用户已确认的新业务规则：订单和工厂单的材料、五金等数据已经直接保存到中央 SQLite；正常订单查看、材料详情、库存查询和出库流程不需要生成或校验 Traveler。
- Traveler 仅在用户选中订单后按需生成，生成内容来自数据库；生成后由用户自行另存到指定目录，或直接打印。Traveler 不再是订单流程的业务事实源、同步前置条件或按钮门禁。
- 当前代码缺口：订单中心仍用 `orderPreviewReady`、`orderCanGenerateTraveler`、`orderExistingTravelerPath` 和 material/Server/Traveler 状态控制按钮，导致数据库中已有订单数据时按钮仍可能全部灰色；生成逻辑也仍沿用旧的临时 Traveler 流程。
- 已完成：订单行单击恢复为展开/折叠所属工厂单，双击打开独立订单详情页面；详情页只显示 SQLite 中的板材/封边和按工厂单分组的五金，并只保留一个“生成 Traveler”按钮。
- 已完成：生成 Traveler 改为调用数据库生成流程，从 SQLite 读取已保存材料和五金，生成到临时目录后直接打开 Excel；不读取 Server/material 文件、不写入订单目录，用户可在 Excel 中自行打印或另存。
- 待完成：库存查询和出库预览目前仍复用旧的 Traveler 文件参数，需继续将库存材料预览和出库写入参数改为直接读取 SQLite，之后才能完全移除其余 Traveler 依赖。
- 本次状态：已修改订单行单击/双击行为、订单详情 UI 和数据库 Traveler 生成代码；165 个发布测试、macOS UI 回归、工作簿端到端、重建、正式安装和已安装 App 界面验收均已完成。
- 待验证：使用本地缓存订单验证无 Traveler 文件时仍可查看详情、查询库存和进入出库预览；选中订单后验证 Traveler 生成、另存和打印均使用数据库数据，且导出失败不改变订单数据库状态。

---

## 第 1 项：库存页按钮 reposition

按钮位于库存状态区右侧、操作记录上方，使用统一尺寸和间距；窄窗口保持自适应。

---

## 第 2 项：文件名悬停滚动

Traveler 文件名和订单名使用 `ScrollingTextOnHover`，仅在文本溢出且鼠标悬停时循环横向滚动。

---

## 第 3 项：顶部横向导航

五个主页面统一由 `TopNavigationBar` 切换，设置与工作区之间保留分隔。

---

## 第 4 项：预览区与默认窗口

预览区最小高度为 320；默认窗口为 1180×860，最小窗口为 900×640。

---

## 第 5 项：浏览器自动化后台运行

用户希望库存查询、商品资料更新和出库等需要浏览器的操作尽量在后台完成，避免 Chrome 窗口弹出或抢占正在使用的应用、键盘和鼠标焦点。

Playwright 默认使用无头模式运行，库存操作不会弹出 Chrome 或抢占用户焦点。设置环境变量 `TRAVELER_BROWSER_VISIBLE=1` 可临时启用可见模式，用于登录验证、验证码处理和网页故障排查；该开关不改变写入操作原有的确认流程。

---

## 第 6 项：服务器离线测试数据

生产文件页和设置页可在服务器与本机测试数据之间切换。切换到本机时自动生成 PP9001 和 CS900 的非生产 Excel 测试文件；自有订单与来料加工分别模拟 `Optimized Orders` 和 `CUT TO SIZE` 目录。测试 Traveler 与备份也隔离在测试根目录，避免写入正式 iCloud 订单目录。

---

## 第 7 项：精简操作记录区域标题

生产文件页和出库页已移除“操作记录”标题，操作记录列表保留在右侧工作区并减少顶部留白。

---

## 第 8 项：默认窗口与首屏布局

- 默认打开尺寸参考用户 2026-08-07 提供的截图（截图像素尺寸 1854×1554），实施时需按 macOS 屏幕可用区域和 Retina 缩放换算合适的 window points。
- 同步压缩页头、卡片间距、卡片内边距和不必要的固定高度，优先让助手页的输入区、等待确认、任务队列、运行状态和执行结果在首屏完整显示。
- 验收标准：App 以默认尺寸打开时，不需滚动鼠标滚轮即可看到当前页面的完整主要操作区；小屏幕上仍保留自适应与滚动兜底。
- 已将默认窗口调整为 1400×980 points，并压缩首屏间距；macOS UI 回归测试通过。

---

## 第 9 项：去除本机测试界面

- 去除生产文件页的“服务器 / 本机测试”分段切换器。
- 去除“本机测试订单”标题、测试状态和所有测试数据入口，页面只展示服务器订单。
- 与第 8 项窗口尺寸及布局优化一起验收。
- 已移除页面中的测试数据入口和切换逻辑，应用只保留服务器数据源。

---

## 第 10 项：AIMES 工厂单名称 fallback

- 先从 Report 中的板材清单读取可用的工厂单号和名称。
- 对本地无法匹配的工厂单号，恢复并接入历史 AIMES Playwright 查询逻辑，批量获取工厂单名称。
- 合并本地与 AIMES 结果后再生成 Traveler；仍查不到的编号才报错。
- 板材、封边条数量仍然只从 `materials.xlsx` 读取，不与工厂单名称匹配逻辑混用。
- 需同时恢复 AIMES 账号、macOS 钥匙串密码和 Playwright 运行环境。
- 已恢复 AIMES Playwright helper 和 Python fallback；实际查询仍需配置账号、钥匙串密码和网络。

---

## 第 11 项：工厂单号与名称本地缓存

- AIMES 成功查询后，将工厂单号→工厂单名称保存到项目数据文件。
- 后续生成 Traveler 时优先使用本地缓存，只对缺失的新工厂单号调用 AIMES。
- 缓存更新需原子写入，避免中途中断损坏对应关系。
- 已使用 factory-names.json 原子缓存工厂单号与名称。

---

## 第 12 项：切换文件时重置操作记录

- 生产文件页左侧切换订单、Report 或其他文件时，清空右侧操作记录。
- 出库页左侧切换文件或订单时执行同样的重置。
- 只保留当前文件/订单的操作记录，避免不同对象的历史信息混在一起。
- 已在切换生产文件/Traveler 时清空对应操作记录数组。

---

## 第 13 项：全局页面头部合并

- 当前每个页面同时存在顶部应用导航区和下方页面标题区。
- 取消下方页面标题区，将其图标、页面标题、副标题放到顶部导航区左侧。
- 原顶部应用名称区被页面信息替换，保留导航、状态标记和右侧操作按钮。
- 与第 8 项首屏布局一并调整，避免重复头部占用垂直空间。
- 已将页面信息并入顶部导航，底部 AppPageHeader 不再占用布局空间。

---

## 第 14 项：待办页图标重设计

- 当前待办页左侧使用单一打勾图标，不能直观表达“任务清单”。
- 重新生成或选择具有待办清单、任务项和完成状态语义的图标。
- 保持与助手、生产文件、出库、设置等页面图标相同的蓝色线性风格和尺寸。
- 已将待办导航图标改为 checklist，与其他线性 SF Symbol 保持一致。

---

## 2026-08-06 新电脑迁移与全面测试

- 重建 arm64 Python 3.12 虚拟环境，修复从 Intel 电脑复制而来的 `pydantic_core` 架构不兼容。
- `pyproject.toml` 明确 setuptools 包发现范围。
- CLI 默认使用项目 `.venv`，不再误用系统 Python 3.9。
- App 构建自动发现 Python、Node 和 Playwright，不再硬编码个人 Codex 缓存路径。
- App 内加入可搬迁的 Python 基础运行时；项目依赖继续隔离在 `.venv`。
- macOS 部署目标统一为 14.0，并迁移到新版 SwiftUI `onChange` API。
- 签名阶段移到 `/tmp`，规避 Documents 文件提供器自动附加 FinderInfo。
- Python 45 项测试、macOS UI 回归、App 构建、签名、内置 Python/Node/CLI 冒烟测试均通过。

## 2026-08-07 待办批量实现与正式安装

- 完成第 7-14 项：操作记录标题精简、窗口调整为 1400×980、移除测试数据入口、AIMES fallback、工厂名称本地缓存、切换文件时重置操作记录、页面头部合并、待办图标改为 checklist。
- 新增学习文档 01-complete-architecture-and-business-flow.md，记录页面、CLI、Skill、文件、函数、返回值和下一步调用链。
- AIMES 名称缓存使用 data/factory-names.json，通过临时文件替换原子写入；账号密码使用 macOS 钥匙串。
- 验证完成：Python 54 项测试、macOS UI 回归测试、App 构建、签名验证。
- 已执行 scripts/install-app，正式 App 已替换为 /Applications/工作流程助手.app。
- 后续每次业务或 UI 修改必须同步更新学习文档对应章节和本文件的完成记录。

## 待办 15-18：设置、出库与状态消息布局

- 设置页的“文件位置”卡片改为较窄布局并放在“运行范围”下方，目标是在默认窗口中完整显示页面内容。
- 系统账户的库存系统密码保存与 AIMES 密码保存按钮使用统一宽度、间距和垂直基线。
- 出库页移除“模拟写入”，将“忽略选中材料”“恢复选中材料”“设置映射”“后台模拟填写”等剩余操作与真实写入按钮按一行布局；同时检查设置映射按钮的 action、disabled 条件、sheet 弹窗状态和映射保存回调。
- 用户最新要求：取消页面顶部独立消息显示；包括成功、失败、进度等消息在内，统一写入并显示在下方消息记录框，避免顶部消息与记录重复显示。
- 第 18 项已实施：出库页移除顶部独立状态消息，消息记录区域单独滚动，预览区域单独滚动，底部操作按钮保持固定；已同步更新业务规则、回归测试和本交接记录。

## 待办 19：AIMES 账户设置

- 设置页显示 AIMES 用户名和密码输入框，并提供独立保存按钮。
- 用户名写入设置文件，密码写入 macOS 钥匙串，不在项目文件或日志中保存明文密码。
- AIMES 工厂单名称查询前检查用户名、钥匙串密码、Node/Playwright 运行环境；缺失时返回明确的配置错误。
- 已完成设置字段、保存按钮和钥匙串写入；修复“保存全部配置”不会保存 AIMES 密码的问题，并增加用户名读取测试。

### 第 19 项确认的修复方案

- 对普通钥匙串和 Data Protection 钥匙串同时清理旧 AIMES 条目。
- 先执行 SecItemUpdate，找不到时再执行 SecItemAdd。
- 保存后使用 SecItemCopyMatching 回读验证，成功后才清空密码输入框。
- 原计划在界面显示具体 Security 状态码；现已废止。界面只提供用户可执行的钥匙串检查和重试说明，底层状态码不得显示给业务用户。
- 保持 AIMES 独立 service：com.pacificpride.traveler-assistant.aimes。
- 用户已确认，后续与下一批设置页改动一起执行、测试、更新学习文档并重新安装 App。

## 待办 20：设置页三卡片布局与按钮对齐

- “文件位置”区域从当前底部整行卡片改为左侧窄卡片，放在“运行范围”下方。
- “运行范围”“文件位置”“系统账户”形成整齐的两列卡片布局，默认窗口内尽量完整显示。
- 库存系统密码保存按钮和 AIMES 密码保存按钮与上方输入框的左边缘对齐，使用统一宽度和垂直间距。
- 本项与第 15、16、19 项一起修改，并同步更新学习文档、UI 回归测试和交接记录。

## 第 21 项：生产文件状态栏按钮对齐

- 已将“打开 Traveler”“生成/更新 Traveler”“查询材料库存”放入右对齐按钮组。
- 按钮组间距压缩为 6pt，状态消息限制单行并使用尾部截断，避免挤压按钮或换行。
- 已通过 macOS UI 回归测试和 Python 测试。

## 第 22 项：AIMES 浏览器运行时修复

- 原因：App 只打包了 Playwright Node 包，没有打包 Chromium 浏览器缓存；helper 默认寻找不存在的 ms-playwright Chromium 路径。
- 修复：tools/aimes_lookup.mjs 优先读取 TRAVELER_BROWSER_EXECUTABLE，其次使用本机 Google Chrome.app 路径，再由 Playwright 处理其他环境。
- 验证：Node 语法检查通过；后续 AIMES 查询会复用本机 Chrome，不再依赖未打包的 Chromium 缓存。

## 第 23 项：右侧订单详情区裁切

- 原因：订单页和出库页右侧内容容器使用固定最小宽度，HSplitView 分栏变窄时内容超过可视区域，左边缘被裁切。
- 修复：右侧详情 VStack 改为 minWidth 0、maxWidth infinity、leading 对齐，跟随分栏宽度布局。
- 验证：macOS UI 回归测试和 Python 测试均通过。

## 第 76 项：订单中心消息队列时间顺序

- 用户观察：订单中心的消息队列没有按时间顺序显示。
- 当前截图可见消息时间从上到下为 `13:27:54`、`13:26:46`、`13:25:54`、`13:27:54`；该显示顺序不符合统一的时间排序直觉。
- 仅记录问题现象和后续目标，暂不预判是消息生成时间、插入顺序、数组排序、异步回调合并还是界面列表排序导致。
- 后续处理：先检查消息模型的时间字段、消息追加/合并路径和 SwiftUI 列表数据源，使用实际 App 复现并补充回归测试；确认原因后再修复。
- 本项目前不修改 UI 或业务逻辑，不构建、不安装 App；待用户明确开始后，与原因诊断和修复一起执行完整测试、构建、正式安装及实际 App 验证。

### 第 76 项补充：临时文件夹失败重试时误报“新增订单”

- 已确认现象：`/Volumes/server/Optimized Orders/inserthood cabinet OLD CNC` 未被用户修改，但处理失败后再次扫描显示“新增订单 INSERTHOOD CABINET OLD CNC 的工厂单文件夹和报表目录”。
- 已确认原因：临时文件夹处理失败时，当前逻辑会删除该文件夹及其报表在 `source_files` 中的索引；下一次重试因找不到历史索引，被 `upsert_source_file()` 判定为 `added`，从而生成“新增”消息。
- 已确认时间线：同一路径在 11:33、13:13、13:26 的失败重试后均生成过相同的“新增”记录；13:26 的处理失败原因为 Traveler 存在未映射材料。
- 后续修改目标：保留失败临时文件夹的文件基线/稳定身份，使未变化的重试不再被判定为新增；只有实际文件新增、删除或元数据变化时才生成对应变化消息；同时调整文案，明确区分“首次发现”“重新处理”和“文件内容发生变化”。
- 本补充仅记录已确认原因和修复范围，待用户明确开始后再修改代码、补充回归测试，并执行完整测试、构建、正式安装及实际 App 验证。

## 第 24 项：PP 基础订单与数字分单严格隔离

- 原因：Agent 工厂单归属校验原先仅使用 `startswith("订单号-")`；因此 `PP0035-2-MASTER` 会被误归入 `PP0035`。
- 修复：`assistant_cli._factory_name_belongs_to_order()` 和 `order_workflow._factory_name_belongs_to_order()` 按完整订单号判断；基础 `PP####` 后紧跟数字的名称视为独立分单，不能混入基础订单。
- 影响：`PP0035-OFFICE` 仍归属 `PP0035`，`PP0035-2-MASTER` 只归属 `PP0035-2`；生成 Traveler 前发现跨订单名称会明确报错。
- 验证：Python 测试 56 项通过；新增基础订单/数字分单边界回归测试。

## 第 25 项：兼容空格分隔的工厂单名称

- 修复：工厂单名称归属校验同时支持 `PP0035-2-MASTER` 和 `PP0035-2 OPENSHELF` 格式。
- 保持：`PP0035-2` 仍不会归入独立订单 `PP0035`。
- 验证：Python 56 项、macOS UI 回归测试、App 构建和正式安装均通过。

## 第 26 项：材料文件归属记录

- 多个 material 文件时，优先按完整订单号匹配文件名；唯一匹配结果自动保存。
- 无法唯一匹配时返回 `material_assignment_required`、候选文件和 `assignment_key`，避免静默使用错误材料。
- 人工确认可通过 `order assign-material --folder ... --order-id ... --materials-file ...` 保存到 `data/material-assignments.json`，后续预览自动复用。
- 五金仍按 Fittingslist 工厂单号自动归属，跨订单名称继续阻止生成 Traveler。

## 第 27 项：混合订单文件夹筛选

- 修复：共享文件夹中出现兄弟订单的板材清单或五金报表时，不再把“发现其他订单”直接作为当前订单错误。
- 当前订单预览按完整订单号保留本订单工厂单，其他订单内容忽略并写入简短提醒；材料文件仍要求唯一匹配或人工分配。

## 第 28 项：旧 Traveler 自动升级模板

- 更新 Traveler 时只强制要求核心 `WorkOrderTraveler` 工作表。
- 缺少新版 `Picking List` 或 `Usage List` 时，从当前模板补齐并继续写入；兼容旧名称 `Pickinglist`。
- 尽量迁移旧文件中的人工五金记录；无法定位旧区块时保留记录到可写区域，不再因模板版本落后直接失败。

## 第 29 项：复制周末本机调试订单数据

- 从服务器复制 `PP0063-2`、`PP0068`、`PP0035-2`、`PP0065`、`PP0067` 到 `data/local-source/Optimized Orders/`。
- 从服务器 `CUT TO SIZE/cs004` 复制到 `data/local-source/CUT TO SIZE/CS004`。
- 本机数据总量约 118 MB；原服务器文件未修改。

## 第 30 项：修正本机调试数据扫描路径

- 原因：`source_root` 指向 `data/local-source` 上级目录，而订单扫描要求直接指向包含 PP 订单文件夹的 `Optimized Orders`。
- 修复：`data/settings.json` 的 `source_root` 和 `server_source_root` 已改为 `data/local-source/Optimized Orders`。
- 验证：订单列表已正确读取 PP0035-2、PP0063-2、PP0065、PP0067、PP0068；CS004 位于同级 `CUT TO SIZE` 目录。

## 第 31 项：下午确认的多订单与材料房间逻辑

- 共享文件夹会识别完整订单号集合，并通过 `preview-related` / `update-related` 分别预览、生成或更新每个订单 Traveler。
- 工厂单按完整工厂单名称归属；`PP0035-OFFICE` 与 `PP0035-2-MASTER` 不再混用。
- material 支持按 `Room/section` 读取房间级板材、封边数据；不同订单只写入其匹配房间，Usage List 保留房间名。
- 同订单多个 material 文件会累计；房间材料与封边完全一致时产生重复计数提醒。
- AIMES 获取恢复为固定读取当前页 50 条/页；格式有效的查询结果进入订单号、工厂单号和工厂单名称映射缓存，格式异常行只在本次结果中提示，缺失映射时仍自动查询 AIMES。

## 第 32 项：房间级板材小数汇总规则

- 房间行允许出现小数板材数量；按订单汇总后再检查整数性，不再因单个房间的 `1.5` 直接失败。
- 例如多个房间合计为整数时正常继续；订单合计仍为小数时才提示人工检查。
- 未标注房间但有数量的行会计入当前订单并在提醒中标记。

## 第 33 项：Color Table 颜色汇总预览

- 预览中的 panel 数量改为按 material 的 `Color Table` 汇总，每种颜色只显示一次。
- 房间行仍用于订单分配和 Usage List 明细，不再让同一颜色在预览中重复出现。

## 第 34 项：封边颜色排序与 panel 对齐

- 封边预览颜色顺序应尽量复用上方 panel 的颜色顺序，方便用户按颜色逐项核对。
- 已完成：封边颜色严格复用 panel 排序后的颜色列表，其他颜色再按名称排序；修复封边仍使用原始字典顺序的问题。

## 第 35 项：Plywood 固定排序

- Plywood 预览固定按“柜体板 → 抽屉板 → 背板”显示，不随文件原始列顺序变化。
- 已完成：Plywood 固定按柜体板、抽屉板、背板显示。

## 第 36 项：生产订单类型按钮居中

- “自有订单 / 来料加工”分段按钮改为在左侧订单栏水平居中，最大宽度 260pt。
- macOS UI 回归测试、构建和正式安装均通过。

## 第 37 项：生产页面显示关联订单预览

- 生产文件页面改用 `preview-related`，共享文件夹中的多个完整订单会合并显示材料、封边和工厂单信息。
- 更新按钮继续调用 `update-related`，分别生成/更新各订单 Traveler。
- macOS UI 回归测试、构建和正式安装均通过。

## 第 38 项：多订单摘要提醒与卡片高度统一

- 订单号包含多个完整订单时，摘要卡片改为警示色并显示关联订单数量和提示图标。
- 订单号、工厂单、材料项目、本机 Traveler 四个摘要卡片统一高度，避免多订单换行导致右侧卡片不齐。

## 第 39 项：摘要卡片三行文字垂直对齐

- 数值行统一预留两行高度，右侧单行数值与左侧多订单号的标题、数值、详情三行垂直对齐。

## 第 40 项：AIMES 不规范订单号提醒与永久忽略

- 从 AIMES 获取数据时，不能只静默过滤订单号不符合当前规则的工厂单；本次获取结束后应向用户显示一份待确认清单。
- 清单至少显示工厂单号、工厂单名称、AIMES 原始销售单名称，以及用户可理解的不符合规则原因，例如“销售单名称不是 PP+4 位数字（可带数字后缀）或 CS+3 位数字”。
- 用户可以选择保留提醒或“忽略此工厂单”。选择忽略后，以工厂单号作为稳定标识写入本地订单数据库，并保存 AIMES 原始信息、忽略时间和忽略状态，不能只保存在本次运行缓存中。
- 后续自动或手工获取 AIMES 时，数据库中已忽略的工厂单继续跳过，不再重复弹出提醒，也不进入看板订单与工厂单列表。
- 应提供可查看并恢复的“已忽略 AIMES 工厂单”记录，避免误操作后无法撤销；恢复后下一次 AIMES 获取重新按订单号规则校验。
- 取消 `test` 自动过滤：工厂单名称或销售单名称包含 `test` 的记录也进入待确认清单，由用户手工决定是否忽略；未经用户确认不得静默过滤。
- 已完成：新增 SQLite 忽略记录表和数据库版本迁移；自动或手工获取 AIMES 后显示待确认清单，支持逐项选择、稍后处理、永久忽略和恢复。
- 已完成：订单号不符合规则以及名称包含 `test` 的记录统一进入人工确认；已忽略工厂单在后续获取中持续跳过，不进入看板，也不重复提醒。
- 已补充数据库迁移、异常识别、重复提醒抑制、忽略/恢复、AIMES 再次获取和 macOS 界面解析回归测试。

## 第 41 项：AIMES 建议归属与助手页订单看板

- 销售单名称无效、但工厂单名称能解析出合法订单号时，待确认清单显示“按订单号处理”，例如 `CS001-Unnamed` 建议归入 `CS001`。
- 用户确认后把建议归属写入 SQLite，立即更新对应订单并在后续 AIMES 获取中复用；管理窗口显示已确认归属并支持撤销。
- 用户澄清：只把 8 张订单统计卡片移到助手页面；搜索、筛选、同步按钮、操作记录和订单表格仍保留在订单中心，顶部“订单中心”导航与默认页面不变。
- 原状态栏中的看板变化消息改为独立、可滚动的“看板操作记录”卡片，扩大显示区域并使用正文级字号。
- 用户进一步调整：所有当前状态也并入消息记录框，删除“看板操作记录”和“最近 N 条”标题，记录框固定一次显示 3 条消息。
- 用户继续调整：所有消息补充更新时间，消息从旧到新并自动滚动到底部；压缩单行高度；消息框上方增加当前操作，空闲时显示最近一次结果。

## 第 42 项：悬停详情定位与关闭规则

- 待处理：悬停菜单必须紧挨鼠标指针出现，不能与指针相距很远；保留“指向消息行停留超过 1 秒后显示、鼠标移动后关闭”的交互规则。
- 已完成：悬停详情按实际全局鼠标位置定位，优先显示在指针右下方，并在屏幕边缘自动翻转和限制位置。

## 第 43 项：操作记录标题行不显示耗时

- 待处理：操作记录列表的标题行显示当前操作内容，不显示“用时”或耗时信息。
- 普通操作记录行继续按现有规则显示操作耗时；本项只针对标题行。
- 已完成：操作记录标题行只显示当前操作或最近结果，不显示耗时；普通消息行继续显示耗时。

## 第 44 项：空五金报表不应提示人工处理

- 待核实：截图中的 `Fittingslist*.xlsx` 可能是五金报表文件存在，但报表内没有内容，当前被归类为“无法读取”并提示“需要人工处理”。
- 如果确认是空报表，应提示用户“某五金报表没有内容”，不再提示人工处理。
- 真正的文件损坏、格式错误或读取失败仍保留人工处理提示，并继续提供相关文件或文件夹入口。
- 已完成：识别为空的 Fittingslist 改为提示“没有内容，已按无五金处理”，不再归入人工处理；真正损坏或结构异常的文件仍提示人工处理。

## 第 45 项：操作记录表述与更新详情待完善

- 待处理：优化操作记录框中的中文表述，使消息语句自然、明确，避免出现“更新订单……的工厂单相关工厂单的工厂单及报表目录”等重复或不通顺的表达。
- 待排查：订单数据更新类消息的悬停详情当前可能为空；需要查明是消息详情没有写入、详情字段未传递，还是悬停展示层未正确读取。
- 修复后，悬停详情应明确显示更新了哪个订单、哪个工厂单，以及具体更新了哪些数据或文件；不能只显示空白悬停框。
- 已完成：优化订单更新消息表述，并把订单号、工厂单号、更新内容和来源文件写入悬停详情。

## 第 46 项：悬停详情进入后保持显示的交互规则

- 待处理：鼠标在消息记录行上停留超过 1 秒后显示悬停详情；鼠标从消息行移动后，若进入悬停详情页面，则继续保留显示，便于用户使用详情页中的进一步操作入口。
- 鼠标移出悬停详情页面后关闭显示；点击详情页中的进一步操作按钮后也关闭显示。
- 没有进一步操作入口的普通详情，仍按鼠标离开消息行后关闭的规则处理。
- 已完成：悬停详情存在操作入口时支持鼠标进入详情页保持显示；移出详情页或点击操作按钮后关闭。

## 第 47 项：Color Table 八种颜色被错误判定为超限

- 已观察：材料文件的 Color Table 中实际存在 8 种 Panel 颜色，包括 Cashmere SM、Cinnamon Triba、Frappe 3、Ivory Oak、Milk Oak、Penelope FA44、Raven Oak、Woodline 3。
- 当前订单校验却提示“materials Color Table 需要 8 种颜色，最多支持 7 种”，与实际模板和 Color Table 内容不一致。
- 待排查并修复：核对 Color Table 的读取范围、颜色数量固定上限或列边界判断，避免把合法的第 8 种颜色误报为超限；应按实际模板支持 8 种，或改为动态读取颜色列。
- 已完成：Color Table 改为读取实际扩展列并自动复制样式，支持第 8 种及更多合法颜色；移除错误的“最多支持 7 种”限制，并补充 8 色回归测试。

## 第 48 项：Server 变化记录需要显示具体变化类型

- 待处理：Server 变化记录不能只显示“文件夹发生变化”或“报表发生变化”，应明确标注是新增、删除还是修改。
- 记录中应同时显示对应订单号、工厂单号、文件或文件夹名称及路径；对于报表变化，尽量说明具体是哪一份报表发生了什么变化，帮助用户判断是否需要进一步处理。
- 已完成：Server 变化记录明确标注新增、修改、删除，并补充订单号、文件/文件夹名称和路径；相关数据更新消息也明确使用“新增、修改、删除”并显示来源路径。

## 第 49 项：操作进行中的消息只显示在标题行

- 待处理：某项操作正在进行时，不应在消息记录列表中显示该操作的过程消息，避免与已完成记录混淆。
- 操作进行中的状态、当前操作说明和耗时只显示在消息框标题行；操作完成后，再按需要在消息列表中显示最终结果记录。
- 已完成：操作进行中时从消息列表隐藏，只在标题行显示当前操作；操作完成后恢复显示最终结果记录，标题行不显示耗时。

## 第 50 项：人工处理悬停详情需支持鼠标移入并保持显示

- 待处理：需要人工处理的消息显示悬停详情后，鼠标从消息行移动到详情面板的过程中不能立即关闭详情。
- 鼠标离开消息行后，应等待约 1 秒并判断鼠标是否已进入悬停详情面板；如果已进入，则保持面板显示，便于用户点击“打开所在文件夹”等操作入口。
- 只有鼠标既不在消息行也不在详情面板时，才关闭悬停详情；如果直接移入另一条消息行，则立即关闭旧详情并重新等待 1 秒显示新详情。
- 已完成：人工处理详情支持鼠标移入后保持显示，离开详情面板约 1 秒后关闭；消息行之间切换时立即取消旧详情并重新计时。

## 第 51 项：订单更新时间去掉日期与时间之间的 T

- 待处理：订单列表“更新时间”当前显示为 ISO 格式，例如 `2026-08-11T13:37:46`。
- 应将中间的 `T` 替换为空格，显示为 `2026-08-11 13:37:46`，提升用户阅读体验。
- 本项本次只记录，不修改界面。

---

## 第 51 项：App 和项目英文名称候选

- 用户选定英文名称候选：`Pacific Pride WorkHub`。
- 待处理：后续再修改 App 显示名称和项目名称；本次只记录，不修改名称。
- Bundle Identifier 继续保持 `com.pacificpride.traveler-assistant`，避免影响现有 TCC 权限和签名识别。

## 第 52 项：当前未解决问题、PP0037 归属修复与 AIMES 精确查询

- 已修复 F2606060141 的历史误报：解析 Fittingslist 时把唯一订单文件夹 `PP0037` 传入候选归属判断，不再因缺少板材清单而把它留为“待确认”。
- 已增加一次性 SQLite 迁移：对已有数据库中真实存在且名称唯一的 PP/CS 订单文件夹自动确认工厂单归属，并写入一条说明性历史记录；本次实际验证已将 F2606060141 从当前问题列表中移除。
- 对仍然没有订单归属的工厂单，先使用 AIMES 工厂单号精确查询，再尝试从工厂单名称解析订单号；仍不能确认时才进入人工处理，不再直接跳过 AIMES 查询。
- 新增 `active_issues` 当前问题表。历史 `sync_changes` 继续保留在数据库中，但 App 启动不再回放历史警告；只在本次会话首次启动时提示仍为 open 的问题。
- 订单中心右上角新增“当前问题”入口，可查看当前未解决问题、打开相关文件夹、自动处理或输入订单号确认归属；问题解决后标记 resolved，不再重复提醒。
- 已完成 94 项发布测试、macOS UI 回归测试、PP0067 workbook 端到端测试；已通过 Aqua Terminal 使用 Apple Development 证书正式安装并验证签名。

## 第 53 项：更新商品资料成功后的弹出提示

- 用户反馈：库存页点击“更新商品资料”并成功后，会弹出提示；截图中可见“更新商品资料”按钮样式。
- 已完成：更新商品资料时显示进行中状态；成功时显示更新数量，失败时显示可操作的失败原因，并恢复按钮可用状态。
- 已修复：库存系统后台浏览器任务结束后正确退出，避免 Swift 一直等待子进程而永久停留在“正在更新商品资料…”。钥匙串、浏览器和 App 外层进程均有超时/失败回传保护。
- 2026-08-12 公司网络实测：验证码安全验证被库存系统要求，App 已正确显示“请先使用可见浏览器完成登录验证，再重试”，未把未完成的在线更新误报为成功。

## 第 54 项：当前问题警示图标按问题数量显示

- 用户要求：存在未处理问题时显示一个警示图标，图标样式和右侧信息图标保持同一视觉风格。
- 没有未处理问题时不显示该警示图标。
- 点击警示图标后继续打开当前问题详情；现有详情显示逻辑不变。
- 已完成：警示图标改为与右侧信息按钮相同尺寸、背景、圆角和边框风格；继续按未解决问题数量控制显示，点击后打开原有详情。

## 第 55 项：设置页整体布局重新整理

- 用户要求：设置页整体布局需要重新调整，以截图为参考，使各设置卡片排列整齐。
- 后续重点：统一“运行范围”“系统账户”“库存商品资料与材料映射”等区域的卡片宽度、间距、标题和控件对齐关系，减少当前上下错落和留白不均的问题。
- 本项只涉及界面布局，不改变设置功能和数据保存逻辑。

## 第 90 项：出库预览页尺寸与操作按钮调整

- 用户提供截图并要求：本页面缩小高度和宽度，以能够完整显示预览信息和操作信息为准。
- 待移除按钮：右上角“更新商品资料”、右上角“刷新列表”、底部“后台模拟填写”。
- 待重命名按钮：“确认写入库存系统”改为“确认出库”。
- 已完成：订单上下文出库窗口调整为紧凑尺寸（735×720，宽度为此前 980pt 的 75%）；右上角两个按钮和底部后台模拟填写入口已移除；三个材料操作按钮与“确认出库”统一排成一排并整齐对齐；最底部写入提示文字已移除。
- 已完成验证：完整发布门禁、构建、正式签名安装和已安装 App 启动检查通过。
- 截图仅作为当前界面布局参考，实际修改范围以本条文字记录为准。

## 第 91 项：工厂单表格单选框尺寸与对齐

- 用户提供截图并要求：工厂单表格第一列前面的单选框变大，并在该列内水平居中。
- 已完成：选择框拆分为独立 54pt 列，图标尺寸放大至 20pt，并在该列水平居中；原有点击整行选择和多选逻辑保持不变。
- 已完成验证：macOS UI 回归测试、完整发布门禁、构建、正式签名安装和已安装 App 启动检查通过。
- 截图仅作为当前界面布局参考，实际修改范围以本条文字记录为准。

## 第 92 项：复用库存系统 Chrome 后的其他出库单菜单定位

- 已确认事实：后续复现时库存专用 Chrome 页面实际打开的是“新增其他出库单”空白表单，路由为 `action=initOi&type=out`；该页面没有日期、备注和查询控件，不能按记录列表处理。
- 已完成：列表识别使用 `action=initOiList` 和真实列表控件；不再直接复用当前新增表单，而是统一从其他出库单记录列表按备注查重，再打开旧单或全新的新增表单。
- 已完成：库存 Chrome 的统一起点改为已登录库存主工作台；若当前停留在 `service.jdy.com/workbench` 服务工作台，自动点击唯一的“进入使用”后等待左侧菜单真正可用，再按操作类型导航。多标签页优先复用已进入业务系统的标签页。
- 已完成：新增表单填写后、保存前回读全部非空明细，校验商品编号、数量和行数，避免上一次失败操作残留的材料被一起出库。
- 已修复：实际复现发现点击“其他出库单”会进入 `action=initOi` 新建表单，而历史查询列表是 `action=initOiList`，不能把新建表单误判为列表加载失败；现在会优先点击当前表单的“历史单据”进入记录列表，再按备注查重。
- 已修复：后台出库操作失败后锁定“确认出库”，必须重新预检后才能重试，避免在失败状态下重复提交或误以为已经进入写入阶段。
- 已完成：菜单、记录列表、出库表单分别等待其关键控件；错误信息区分左侧菜单未加载、记录列表未加载和表单未加载，不再统一泛化成菜单错误。
- 已完成验证：本地测试覆盖 Chrome 复用与菜单导航代码；正式 App 已安装并启动检查通过。当前未在真实库存系统中点击“确认出库”，因此真实业务单据写入仍需用户在库存系统登录状态正常时人工验证。
- 已完成：设置页整理为三列布局，左列放置“运行范围”和“文件位置”，中列放置“系统账户”，右列放置“库存商品资料与材料映射”；统一卡片间距、宽度和顶部对齐。

## 第 56 项：订单中心按 AIMES、出库状态和临时任务筛选 Server 扫描范围

- 已完成：标准订单扫描候选改为 AIMES 历史/最新工厂单记录与本地出库状态驱动；只有全部工厂单明确“已出库”时才跳过，AIMES 新增工厂单会重新触发所属订单扫描。
- 已完成：非标准名称文件夹按临时任务处理，使用 `CS003 PP0047` 建立时间 `2026-07-25 11:00:25 -0700` 作为基准；基准时间后一段时间内或从未处理的临时文件夹进入自动扫描，并递归识别 `Fittingslist`、`板材清单`、`material` 文件。
- 已完成：Server 扫描不再因为顶层目录时间判断标准订单；未被本次扫描的标准订单不会自动关闭历史 `active_issues`。
- 已完成：AIMES 获取失败继续使用最近一次缓存执行后续 Server 扫描，并在订单中心弹出醒目失败提示。
- 本次本地验证：Python 99 项测试、macOS UI 回归测试通过。
- 待明天上班后验证：真实 `/Volumes/server` 候选订单数量、临时文件夹实际结构、AIMES 新增工厂单触发 Server 扫描，以及正式 App 启动后的完整流程。

## 第 57 项：当前操作文案去掉“手工”

- 用户要求：订单中心当前操作显示“正在手工获取 AIMES…”时，改为显示“正在获取 AIMES…”；后续涉及同类“正在手工获取……”文案时一并统一为“正在获取……”。
- 本项只调整显示文案，不改变自动获取、手工获取或其他操作的实际逻辑。
- 已完成：手工获取 AIMES 的进行中文案改为“正在获取 AIMES…”，获取逻辑保持不变。

## 第 58 项：顶部四页标题字体增大

- 用户要求：顶部导航区域的四个页面标题字体适当增大，例如“订单中心”。
- 本项只调整顶部页面标题的字号，不连带修改副标题或顶部导航按钮文字；具体字号后续修改时结合实际 App 窗口效果确认。
- 已完成：顶部页面标题字号从 17pt 调整为 19pt，副标题和顶部导航按钮文字保持不变。

## 第 59 项：库存系统钥匙串读取与设置页状态提示

- 已确认原因：钥匙串中存在库存系统密码，但正式 App 内 `keychain-read` 位于资源目录且未被 `codesign --deep` 签名；macOS 将其以 `SIGKILL` 终止，App 因此误报“密码尚未保存”。
- 已完成：`scripts/install-app` 显式签署并验证 `keychain-read`，安装后用不存在的账号执行不输出密码的探测，防止未签名辅助程序再次漏过发布门禁。
- 已完成：设置页使用统一的成功、错误、进行中状态横幅；商品资料更新结果只显示在商品资料卡内，不再与页面底部重复显示。
- 验证完成：102 项发布测试、macOS UI 回归、PP0067 workbook 端到端测试、App 构建、Apple Development 签名安装和 `/Applications/工作流程助手.app` 深度签名验证均通过；真实 App 已确认更新入口不再立即报“密码尚未保存”。
- 未验证：本次真实 App 的库存系统在线更新在超过一分钟后仍处于进行中，未将其结果表述为成功。

## 第 60 项：库存系统登录被安全验证拦截

- 已确认失败步骤：库存系统密码已从钥匙串成功读取，登录页也成功打开；提交账号密码后，`/commonservice/ajaxChecking.do` 返回了 `challenge`、`sessionId`、`type` 和 `gt`，随后页面回到登录页，尚未进入工作台，也没有开始下载商品资料。
- 已完成：Playwright 检测到安全验证挑战后，后台模式立即返回明确错误；可见模式提示用户完成验证码、滑块或扫码验证，并等待更长时间供人工完成。不会尝试绕过库存系统的验证码。
- 已验证：正式安装 App 的 CLI 已复现并返回“请先使用可见浏览器完成登录验证，再重试”；真实 App 更新入口也显示同一明确提示。103 项 Python 发布测试、macOS UI 回归、PP0067 workbook 端到端测试、构建、签名和正式安装均通过。
- 待用户操作：在正常 macOS Terminal 中运行 `TRAVELER_BROWSER_VISIBLE=1 /Applications/工作流程助手.app/Contents/Resources/project/scripts/traveler-assistant inventory preflight`，在弹出的可见浏览器中完成安全验证后，再回到 App 重试“更新商品资料”。

## 第 61 项：优先复用已打开的库存系统 Chrome 页面

- 用户建议：进入库存系统前，先检查已经打开的浏览器；如果已经进入库存系统，则直接使用该 Chrome 完成任务。
- 已完成：库存助手先尝试连接程序启动并开放本地 CDP 端口的 Chrome，发现已登录的 `service.jdy.com` 页面后复用当前页面；连接不到或页面未进入库存系统时，继续使用原有受控浏览器流程。
- 安全边界：普通用户直接打开的 Chrome 默认没有远程控制端口，程序不会强行接管；只有由本程序启动或明确配置 `TRAVELER_CHROME_CDP_ENDPOINT` 的 Chrome 才会被复用。复用远程浏览器结束时只断开调试连接，不关闭用户的 Chrome。

## 第 62 项：库存系统全球站退出页登录入口兜底

- 真实 App 复现：点击“更新商品资料”后，浏览器跳转到 `www.jdy.com/global/?logout=true`；原逻辑因为 URL 仍包含库存系统域名，错误地认为已有登录状态，最终报告“未找到登录表单”。
- 已完成：全球站未显示 `Sign in` 时直接打开 `https://service.jdy.com/login/`；后续仍停留在全球站时返回明确的登录入口错误，不再误报为已登录。
- 待验证：重新保存当前库存系统钥匙串密码后，继续做真实的“已登录 Chrome 页面复用 → 商品资料导出”端到端测试。

## 第 63 项：保持受控 Chrome 供后续任务复用

- 已发现缺口：受控 Chrome 虽然启动了本地 CDP 端口，但任务结束时原逻辑会关闭浏览器，下一次任务无法复用已登录页面。
- 已完成：正常受控 profile 的 Chrome 任务结束后只断开 Playwright 连接并保持浏览器进程；下一次任务可通过 CDP 先寻找库存工作台页面。临时 profile 仍会关闭并清理，避免残留临时数据。
- 未完成：普通用户直接启动、没有 CDP 端口的 Chrome 仍不能被程序安全接管；真实复用测试需要一个由程序启动或用 `TRAVELER_CHROME_CDP_ENDPOINT` 明确开放调试端口的 Chrome。
- 安全补充：任务失败时关闭本次受控 Chrome；只有成功完成任务才保持浏览器进程，避免后台验证码失败后阻塞可见浏览器恢复流程。

## 验证命令

```bash
PYTHONPATH=".:vendor" .venv/bin/python3 -m unittest discover -s tests -v
./scripts/test-macos-ui
./scripts/build-app
```

## 第 64 项：CS003 PP0047 混单文件夹不应按临时文件夹处理

- 用户反馈：Server 扫描弹窗将 `CS003 PP0047` 识别为“临时订单文件夹”，但该目录名称和内容可以解析为混单文件夹，不应进入临时文件夹处理流程。
- 已完成：名称可解析出两个及以上完整订单号的非标准目录按混单文件夹处理；`CS003 PP0047` 不再显示为临时订单，也不再走临时订单路径。
- 已完成：真正的临时文件夹在用户点击“自动处理”后校验文件格式，优先读取匹配订单号的 material；缺少时从 Report 板材清单生成 material，读取五金清单，生成或更新 Traveler 并尝试库存出库。
- 已完成：临时文件夹格式不合规、订单无法识别、解析失败、材料映射失败或出库失败时写入当前待处理问题，并保留 Server 变化基线，下一次扫描继续提醒；“稍后处理”也继续保留提醒。
- 已完成：新增混单识别、临时处理成功、临时处理失败留存和出库调用回归测试；待发布门禁和真实 `/Volumes/server`、库存系统、正式 App 流程验证完成后关闭本项。

## 第 65 项：Server 待处理按文件夹分组并支持选择性处理

- 用户反馈：点击“稍后处理”后顶部没有显示待处理警示图标；弹窗按文件逐条显示，无法选择某个文件夹单独自动处理。
- 已确认原因：顶部警示图标原来只绑定 `currentIssues`，没有绑定仍保留在内存中的 `pendingServerChanges`；Server 弹窗数据模型也只有文件级记录。
- 已完成：顶部新增 Server 待处理警示入口；Server 弹窗按来源文件夹分组，显示文件夹下新增、修改或删除的文件明细，支持全选、取消全选和逐文件夹勾选。
- 已完成：自动处理命令接收所选文件夹，只处理勾选项；未勾选文件夹继续保留在待处理清单。补充文件夹分组、选择和选择性处理回归测试。
- 待验证：重新安装 App 后进行真实 Server 扫描、点击稍后处理、重新打开警示入口，并选择单个文件夹执行自动处理。

## 第 66 项：待处理中心显示文件变化时间

- 用户要求：待处理中心按文件夹展开文件变化明细时，新增文件显示文件创建时间，修改文件显示文件修改时间，帮助用户判断变化详情。
- 已完成：Server 文件变化记录及待处理中心展开明细保留对应时间；新增使用创建时间，修改使用修改时间，删除文件按记录时间展示。
- 已完成：时间显示改为与文件名同一行，便于用户直接识别具体变化。
- 已验证：相关 UI 回归测试、发布门禁、构建和正式安装完成。

## 第 67 项：出库前选择是否包含五金件

- 用户要求：手工选择文件夹处理、Server 扫描弹出的待处理列表、待处理中心弹出的列表，这三种入口在出库前都支持选择是否包含五金件。
- 业务规则：临时订单和代客加工订单有时不需要五金件；如果选择“不包含五金”，生成或写入 Traveler 时不得写入任何五金信息，即使 Server 报表文件夹中存在 Fittingslist 或其他五金报表。
- 出库规则：库存出库完全按照最终 Traveler 文件的内容执行，不再依据报表文件夹中的五金信息额外出库。
- 已完成：统一手工处理、Server 扫描待处理和待处理中心三种入口的五金选择状态，并传递到 Traveler 生成/更新逻辑。
- 已完成：关闭“出库包含五金件”时不写入 Traveler 五金信息，出库只读取最终 Traveler 内容。
- 已验证：五金排除、Traveler 内容和出库调用回归测试通过；本次发布门禁、构建和正式安装完成。

## 第 69 项：无报表混单文件夹检查与忽略

- 用户要求：像 `CS003 PP0047` 这类名称可识别为混单、但内部没有报表文件的 Server 文件夹，不应进入自动处理；应提示用户检查，并允许用户忽略。
- 已完成：扫描时生成 `server_missing_report` 问题，统一进入待处理中心，显示“报表检查”和具体缺少报表原因；该记录不能勾选自动处理，可打开文件夹检查。
- 已完成：忽略后保存文件夹基线并观察 30 天；观察期内文件夹元数据或可识别报表状态发生变化时解除忽略并重新提醒；30 天内没有变化则转为永久忽略。
- 已完成：如果观察期内出现板材清单、material 或 Fittingslist，系统关闭缺少报表问题，并显示新增报表变化，等待用户处理。
- 已验证：新增无报表提示、忽略后不重复提醒、30 天内变化重新提醒、报表后来出现和观察期过期永久忽略回归测试；完整发布门禁、构建、正式安装、签名和已安装 App 启动验收通过。
- 未验证：没有对生产 `/Volumes/server` 中的真实 `CS003 PP0047` 执行忽略操作，以避免修改生产状态；生产场景下需由用户在 App 中检查后点击“忽略此文件夹（观察一个月）”。

## 第 68 项：统一时间显示格式

- 用户要求：App 内所有时间显示统一去掉 ISO 时间中的 `T`，在日期和时间之间使用空格。
- 显示规则：例如 `2026-08-12T10:00:00` 显示为 `2026-08-12 10:00:00`；只调整显示格式，不改变底层时间值、排序或比较逻辑。
- 已完成：集中使用 `appDisplayTimestamp` 处理订单、Server 扫描、待处理中心、AIMES 和操作记录中的日期时间显示；只改变显示，不改变底层时间值、排序或比较逻辑。
- 已验证：相关 UI 回归测试、发布门禁、构建和正式安装完成。

## 第 70 项：启动时待处理中心过早弹出且操作被锁定

- 用户反馈：刚打开 App、后台仍在扫描 Server 时，待处理中心已经弹出；此时“稍后处理”、忽略文件夹和自动处理等按钮均为灰色，用户无法操作。
- 已确认原因：启动流程先读取本地订单索引缓存；`list-index` 返回后立即根据缓存的 `pendingCenterItems` 设置 `showPendingCenterPrompt = true`，随后才开始后台 AIMES/Server 检查。因此弹窗显示时，后台操作仍在运行，底部按钮因 `orderRunning` 被禁用。
- 已完成：启动缓存阶段不再自动弹出待处理中心；后台 AIMES/Server 检查完成后才根据最新结果弹出待处理中心，避免旧缓存误导用户。
- 已完成：待处理中心“稍后处理”始终可点击，即使后台扫描仍在运行也可以关闭弹窗；自动处理、忽略等实际业务操作仍按扫描状态和选择条件正确禁用。
- 已验证：已安装 App 实际扫描期间保持主界面、没有提前弹出待处理中心；扫描完成后待处理中心正常显示，“稍后处理”可关闭。完整发布测试、构建、签名和正式安装均通过。

## 第 71 项：待处理中心文件时间显示位置与格式

- 用户反馈：文件变化明细中，时间当前单独显示在文件名下一行，影响阅读；并且日期和时间之间仍显示 `T`，没有按要求替换为空格。
- 已完成：将时间放到“新增/修改/删除：文件名”同一行的后面；统一把显示格式从 `2026-07-31T14:57:15` 改为 `2026-07-31 14:57:15`。
- 约束：只修改界面显示，不改变底层时间值、排序、扫描比较或数据存储格式。
- 已验证：UI 回归测试、发布门禁、构建和正式安装完成。

## 第 72 项：操作记录悬停详情混入无关信息

- 用户反馈：鼠标悬停在“Excel 文件无法读取”这一条操作记录上时，详细信息显示了很多与当前消息不相关的内容；当前记录还直接显示了底层 Python 局部变量错误文本。
- 已完成：悬停详情只绑定当前消息的业务原因和来源路径，不再把其他 Server/AIMES 操作的路径、上下文或明细聚合进来；详情明确显示具体失败文件名。
- 已完成：修复 `order_hint` 未初始化导致的 Excel 读取错误，使格式异常回到对应报表文件的业务错误分支，不再把 Python 局部变量错误直接显示给用户。
- 已验证：补充错误文件路径和详情隔离回归测试，并通过发布门禁。

## 第 73 项：临时订单使用文件夹名作为身份并防止重复出库

- 用户确认：没有 AIMES 关联的临时订单，Traveler 的订单号、Usage List 订单号和工厂单名称都使用文件夹名；工厂单号可以留空；五金可以按用户选择写入，出库备注使用文件夹名。
- 已完成：临时文件夹通过路径、文件系统 inode 和创建时间生成稳定内部身份，并在 SQLite `temporary_orders` 中记录文件夹、内容指纹、Traveler 路径、处理状态、出库状态、出库单号及时间。
- 已完成：同一文件夹内容未变化且台账已记录“已出库”时，重复扫描、待处理中心处理或 Server 选择处理不会再次调用库存出库；文件内容发生变化时重新进入处理流程。
- 已完成：AIMES 中销售单名称不符合 PP/CS 规则的记录持久化为待确认候选；临时文件夹名与 AIMES 工厂单名称唯一匹配时自动使用工厂单信息，多条匹配仍要求人工确认；无匹配时按纯临时订单处理。
- 已完成：临时订单扫描阶段不再把辅助 Excel 或缺少标准工厂单号的报表误报为普通订单报表异常；实际处理阶段再按“有 AIMES 匹配/无匹配”选择严格或非限定解析。
- 已验证：Python 全量回归测试 118 项、macOS UI 回归、构建、Apple Development 签名、正式安装和已安装 App 验收均通过；待用户用实际临时订单和库存系统执行生产流程验收。

## 第 74 项：当前操作提示显示订单身份

- 用户要求：后台显示“正在自动处理 Server 变化、解析报表并准备出库…”等操作提示时，必须同时显示当前处理的订单号。
- 显示规则：标准订单显示订单号；混单显示涉及的订单号；没有标准订单号的临时订单显示对应文件夹名称。
- 约束：本项仅记录待后续修改，当前不改代码、不构建、不安装 App。

## 第 75 项：出库失败重试复用未变化的 Traveler

- 用户要求：临时订单出库失败后再次处理时，先判断报表内容和五金选项是否变化；如果没有变化，不要无谓地重新更新 Traveler，直接使用现有 Traveler 重试出库。
- 已完成：临时订单台账新增 Traveler 指纹、生成时五金选项、Traveler 状态和生成时间；生成或更新 Traveler 成功后立即记录这些信息。
- 已完成：重试时在报表指纹、五金选项、Traveler 状态和文件存在性均匹配时复用现有 Traveler；报表变化、五金选项变化、Traveler 缺失或未成功生成时仍重新校验并更新。
- 已验证：新增“第一次出库失败、第二次复用未变化 Traveler”回归测试；119 项 Python 测试、macOS UI 回归、构建、正式安装和已安装 App 启动验收均通过。实际库存系统验证码恢复和生产订单出库仍需用户现场验收。

## 第 77 项：汇总全部对话中的业务逻辑并建立同步约定

- 用户要求：将所有对话中已经确认的业务逻辑集中同步到业务规则 Markdown；以后任何对话中修改业务逻辑时，也必须同步更新业务规则文档。
- 已完成：补充 `docs/business-rules.md` 的临时订单身份、AIMES 匹配、Server 分类、统一待处理中心、时间显示、失败重试指纹、重复出库保护、验证码边界和文档维护约定。
- 已完成：同步补充 `docs/inventory-outbound-rules.md` 的临时订单备注、五金开关、Traveler 复用、临时订单台账和安全验证规则。
- 约定：只讨论但尚未授权实现的规则标记为“待实现”或“待确认”；实现时同时更新代码、业务规则文档和本交接记录。

## 第 78 项：订单中心悬停菜单不显示操作按钮

- 用户要求：订单中心消息记录的悬停菜单不需要显示操作按钮。
- 统一入口：所有操作按钮和人工处理动作统一放到“待处理中心”，悬停菜单只保留信息查看，不提供操作入口。
- 已完成：移除订单消息行中的“打开待处理中心”操作按钮，并移除悬停详情中的“打开所在文件夹”操作按钮；操作统一保留在待处理中心。
- 已完成：修复手动获取 AIMES 时的弹窗条件；仅有 Server 待处理项目时不再因 AIMES 完成而弹出待处理中心，存在 AIMES 待确认记录时仍可提示。
- 已验证：124 项 Python 测试、macOS UI 回归、构建、签名、正式安装和重启 App 均通过。

## 第 79 项：订单展开区 Panel 颜色单行显示

- 用户要求：点开订单后，Panel 颜色从当前两行显示改为一行显示，并适当加大字体。
- 已完成：Panel 颜色摘要改为单行显示并适当加大字体。

## 第 80 项：工厂单明细表从左侧开始排列

- 用户要求：点开订单显示工厂单明细时，表格整体不要居中，保持从左侧开始显示；每一列的标题和具体内容在各自列内居中。
- 已完成：工厂单明细表整体保持从左侧开始排列；每列标题和内容在各自列内居中。

## 第 81 项：Server 自动与手工扫描同时覆盖两类订单目录

- 用户确认：根据订单号判断自有订单或来料加工，分别进入 `Optimized Orders` 或同级 `CUT TO SIZE`；App 自动扫描 Server 和手工点击“扫描 Server”都必须同时扫描两个目录。
- 已完成：订单索引、Server 变化扫描和批量处理共用双目录路由；CS 订单成功通过板材/材料校验后，使用已确认归属的工厂单更新优化状态。自动或手工扫描完成后都会刷新本地订单索引，使已有的 CUT TO SIZE 优化产物及时反映到看板；即使存在待处理变化也保留待处理清单，仍不会自动处理报表、生成 Traveler 或出库。
- 已验证：增加双目录快照扫描和 CS005 类来料加工订单优化状态回归测试；实际挂载的 `/Volumes/server/CUT TO SIZE/cs005` 已识别 `New Nesting/Optimize file/Optimize file.xml`，并将 CS005 更新为 `已优化`、优化进度 `1 / 1`，材料状态保留为 `待校验`。

## 第 82 项：已优化但没有 material/Traveler 时允许一键生成

- 用户确认：订单已优化但本机还没有 Traveler 时，订单中心的“生成 Traveler”不应因为缺少 material 而灰掉；点击后应自动从 Server 报表生成 material，校验成功后继续生成 Traveler。没有实际 Traveler 文件时，“打开 Traveler”必须禁用。
- 已完成：订单中心选中已优化订单后，缺少 material 也允许进入生成流程；先写入 Server 订单文件夹中的 `<订单号> materials.xlsx`，重新校验后继续执行 Traveler 生成。生成条件仍要求订单文件夹、订单号和优化证据有效；失败时停止，不写半成品、不自动出库。
- 已完成：打开按钮改为检查 Traveler 文件实际存在，而不是只检查内存中的路径字符串。
- 已验证：新增 macOS UI 回归覆盖“已优化+缺少 material 可生成”“未确认已优化不可绕过预检”和“没有 Traveler 时打开按钮禁用”。

## 第 82 项：优化状态优先依据有效板材清单

- 用户要求：暂不修改，后续与其他问题一起处理。
- 待修改规则：判断工厂单“已优化”时，应优先检查报表文件夹中的有效板材清单，而不是只判断 `Optimize file/Optimize file.xml` 或 `nesting_result.xml`。板材清单必须能够读取、结构有效，并能匹配当前订单或工厂单；优化文件可作为辅助证据。
- 当前状态：仅记录待办，未修改代码、未重新构建、未安装 App。

## 第 83 项：手工登录库存专用 Chrome 后由 App 复用

- 用户确认：可以先在 Chrome 中手工登录库存系统、完成验证码，再让 App 继续库存查询、商品资料导出或出库操作，不应每次重新登录。
- 已完成：设置页新增“打开库存专用 Chrome”；该 Chrome 使用独立用户资料和本地 CDP 端口，打开后由用户手工登录库存系统。
- 已完成：库存操作开始前读取本地 CDP 标签页列表；发现已登录的 `service.jdy.com/workbench` 页面时直接复用，不读取或传递库存系统钥匙串密码。未发现时保留原有钥匙串 + Playwright 登录路径。
- 安全边界：普通用户直接启动、没有 CDP 端口的 Chrome 不由 App 强行接管；操作结束只断开调试连接，不关闭库存专用 Chrome。验证码、滑块或安全验证仍由用户在该 Chrome 中完成，不绕过网站安全验证。
- 待验证：正式安装 App 中点击“打开库存专用 Chrome”后，手工登录并执行一次真实库存只读查询；真实出库仍需单独审批和现场验收。

## 第 84 项：修正库存专用 Chrome 登录入口和工作台域名识别

- 用户现场反馈：点击“打开库存专用 Chrome”并手工登录后，更新商品资料仍失败；实际登录入口是 `https://www.jdy.com/login/`，不是 `https://service.jdy.com/login/`。
- 已确认原因：操作日志显示更新商品资料时“未发现已登录页面”，随后回退到新的 Playwright 登录流程并触发验证码；问题是入口/页面识别条件遗漏，不是用户登录失败。
- 已完成：启动入口改为 `https://www.jdy.com/login/`；Python CDP 探测和 Node 页面复用同时识别 `www.jdy.com`、`service.jdy.com` 的工作台页面，并继续排除登录页和全球站退出页。
- 待验证：重新安装版中手工登录后执行一次真实商品资料更新；成功时操作记录应显示直接复用已登录页面，且不再读取钥匙串密码或启动第二个浏览器。

## 第 85 项：兼容库存系统租户工作台地址

- 用户现场截图确认：手工登录后页面实际地址为 `https://vip2-hz.jdy.com/default-new.jsp?...`，不是固定的 `service.jdy.com/workbench/` 或 `www.jdy.com/workbench/` 路径。
- 已确认原因：此前 Python 和 Node 的 CDP 页面探测只接受固定工作台路径，因此把已经登录的租户工作台误判为未登录，随后错误回退到 Playwright 登录流程并触发验证码。
- 已完成：Python 和 Node 同时改为识别 `jdy.com` 及其子域名的 HTTP(S) 应用页面；继续排除登录页、退出页和全球站退出状态；复用成功日志记录实际页面 URL。
- 待验证：重新安装版中手工登录后执行一次真实商品资料更新；日志应显示复用 `vip2-hz.jdy.com/default-new.jsp` 页面，且不再读取钥匙串密码或启动第二个浏览器。

## 第 86 项：修复复用 Chrome 导出成功后的清理错误

- 用户现场反馈：页面复用和商品资料导出均已完成，但 App 很快报告失败。
- 已确认原因：商品资料下载完成后，Node 清理 CDP 连接时调用了不存在的 Puppeteer 方法 `remoteBrowser.disconnect()`，导致 Playwright 抛出 `TypeError`；Python 因此没有执行商品资料导入，并删除了临时下载文件。
- 已完成：改用 Playwright 的 `Browser.close()` 清理 CDP 客户端连接；对已连接的用户 Chrome 不执行退出浏览器操作，并覆盖“未找到可复用页面”的连接清理分支。
- 待验证：重新安装版中执行一次真实商品资料更新；应完成下载和本地导入，操作记录显示成功，库存专用 Chrome 保持打开。

## 第 87 项：商品更新摘要和 App 退出时关闭专用 Chrome

- 用户确认：商品资料更新成功后需要看到新增、更新、删除和最终总数摘要；更新成功时不关闭库存专用 Chrome，但正常退出 App 时关闭本次 App 启动的专用 Chrome。
- 已完成：Python 比较更新前后商品编号及资料字段，返回 `added_count`、`updated_count`、`removed_count`；Swift 成功状态显示摘要。
- 已完成：新增 `close-chrome` 退出命令，使用 CDP `Browser.close` 关闭专用 Chrome；只在 App 本次通过“打开库存专用 Chrome”启动了浏览器时执行，普通 Chrome 不受影响。
- 限制：强制退出、崩溃或断电时无法保证退出清理；如果 App 只是复用外部已经打开的专用 Chrome，不会代替用户关闭它。
- 待验证：正式安装版中确认成功提示显示摘要，并确认正常退出 App 后库存专用 Chrome 关闭、普通 Chrome 保持打开。

## 第 87 项：Server 订单索引增量更新

- 用户反馈：Server 轻量扫描约 10 秒可接受，但“更新 Server 订单索引”重复耗时约 64 秒。
- 已确认原因：同步阶段会登记并解析当前 112 个业务 Excel，并对 13 个已有 AIMES 订单全部调用 `preview_order`；此外，原实现把纳秒修改时间写入 SQLite 的 REAL 列，浮点精度丢失会使未变化文件无法可靠复用。
- 已完成：未变化的板材清单和 `Fittingslist` 复用 SQLite 缓存；只有新增、修改或删除报表重新校验受影响订单；优化产物做轻量检查；修改时间改为毫秒标记；新增 `sync-index --full-refresh` 作为完整校验入口。
- 已验证：项目 Python 回归测试 141 项通过；增量回归覆盖首次解析、无变化复用、文件修改后重新解析以及已有“已优化”状态保留。
- 待验证：重新安装的正式 App 在实际 `/Volumes/server` SMB 挂载上再次执行“扫描 Server”，确认无变化时索引更新时间及看板状态；本机 fixture 测试不替代生产 Server 验证。

## 第 88 项：App 生成 material 后的 Server 变化基线

- 用户反馈：生成 CS005 Traveler 后重新进入 App，提示 Server 上的 CS005 material 发生变化。
- 已确认原因：订单原来没有 material；生成 Traveler 流程从 Report 自动创建 `/Volumes/server/.../CS005 materials.xlsx`。启动扫描按路径、修改时间和大小比较时，将 App 自己新增的文件识别为 Server 待处理变化。
- 已完成：生成 material 成功后，仅登记订单文件夹和本次生成的准确 material 文件；Color Table 的明确自动修正也登记准确文件。没有把同目录其他板材清单或 Fittingslist 一并标记为已处理。
- 已完成：新增回归测试，确认 App 生成 material 后立即扫描无待处理变化；之后实际修改 material 仍报告 `modified`。
- 待验证：重新安装正式 App，在实际 `/Volumes/server/CUT TO SIZE/cs005` 上重新生成或检查一个 material 后重启 App，确认不再出现由 App 自己生成文件引起的提示；真实 Server 验证不由 fixture 测试替代。

## 第 89 项：订单中心悬停详情摘要化读取文件信息

- 用户反馈：悬停菜单中的“读取文件或文件夹”把 Server 下的每个绝对路径全部展开，内容过长，不方便查看；希望类似 `CS001` 文件夹及其下属 material 文件能够简要显示。
- 已确认原因：`sync_order_index` 原来把文件夹和每个报表的完整绝对路径拼接成一行 `operation_trace`，SwiftUI 悬停面板再原样显示。
- 已完成：读取详情改为按文件夹归纳，显示文件夹总数、相关文件总数以及少量示例，并按 `material`、板材清单、五金清单分类计数；不再在批量摘要中显示完整绝对路径。
- 已完成：悬停面板改为 520 点宽、最多 560 点高并支持滚动，各条详情使用项目符号分段显示；具体 Excel 读取失败仍保留文件名，方便定位问题。
- 已验证：新增 Server 读取摘要的文件夹分组、文件类型计数和示例数量限制测试；150 项 Python 测试、macOS UI 回归、Workbook E2E、构建、签名、正式安装和安装版启动/退出检查均通过；安装版内置 Python 摘要输出符合预期。
- 待验证：真实 `/Volumes/server` SMB 挂载上的悬停菜单视觉效果和实际订单数据展示仍需现场复测；fixture 和安装版摘要检查不替代真实 Server 现场验证。

## 已完成：隐藏“打开库存专用 Chrome”后的登录提示

- 用户要求：点击“打开库存专用 Chrome”后，不显示“库存专用 Chrome 已打开，请手工登录并完成验证码后再操作”这行提示。
- 已完成：成功提示不再显示在设置页；只有打开失败时保留错误提示，方便定位问题。
- 相关实现位置：`traveler_assistant/inventory.py` 的 `open-chrome` 返回消息，以及 `macos/TravelerAssistant.swift` 的 `openInventoryChrome()` 状态显示逻辑。
- 已验证：完整发布测试、构建、签名安装和已安装 App 启动检查通过；未执行真实库存系统出库。

## 第 96 项：补齐数据库订单事实并修复同步边界

- 已确认：数据库迁移完成后，`workflow.sqlite3` 有订单和工厂单，但 `material_items`、`hardware_items` 为空；昨天拷贝的 `/Users/lantian/Downloads/server` 包含 13 个可按标准订单目录处理的订单文件夹。`mario extracabinets` 和 `CS003 PP0047` 未自动处理，分别保留临时订单和混单人工边界。
- 已完成：使用本地 AIMES 缓存恢复工厂单归属和拆单信息；从 Server material、板材清单和 Fittingslist 解析并写入中央 SQLite；使用本地库存出库同步记录恢复出库单据和工厂单状态，不执行库存系统写操作。
- 已完成：修复标准订单根目录被混合工厂报表覆盖、CUT TO SIZE 五金误写入、校验连接事务锁和选定目录误校验旧路径的问题。
- 已验证：正式数据库写入 88 条材料事实、105 条自有订单五金事实；CUT TO SIZE 五金为 0。CS001、CS003、CS004、CS005 及 PP0035、PP0035-2、PP0046、PP0047、PP0063-2、PP0065、PP0067、PP0068 的材料/校验已恢复；CS002 因拷贝中缺少 material 文件，仍为“待校验”。
- 已验证：167 个 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、签名安装和安装版启动/退出检查通过。
- 未验证：本次未在线刷新 AIMES，也未查询实时库存余额；结果使用已有 AIMES 本地缓存、库存本地目录和本地出库记录。实时系统中的新拆单、新库存余额或未记录的历史出库需要后续单独只读复核。

## 第 97 项：修复 App 启动清空订单材料事实

- 已确认原因：App 启动时执行 `assistant --usage`，旧版 `assistant_cli.py` 将中央业务库 `workflow.sqlite3` 同时交给 `RuntimeStore` 保存助手用量；`RuntimeStore` 发现中央库版本不是助手专用版本 3 后执行 `path.unlink()`，随后重建数据库，导致 `material_items`、`hardware_items` 消失，而订单索引在后续启动流程中又被迁回，所以表现为“状态仍在但详情没有材料”。
- 已完成：助手用量和学习命令改存独立的 `assistant-runtime.sqlite3`；未知运行库版本改为报错并保留原文件，禁止误删业务数据库。
- 已验证：CS005 材料 5 条、中央材料事实 88 条、五金事实 218 条；新安装 App 启动后数据库仍保持上述数量，CS005 详情页显示柜体板、抽屉板、背板、Plywood、Woodline 4 和 Edge Banding 明细。
- 已验证：170 个 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版 App 启动/详情行为验证通过。

## 第 90 项：已出库工厂单禁止重复出库，数据库事实变化时允许确认更新

- 用户确认的业务规则：如果订单索引 SQLite 查询到准确工厂单已出库，订单中心不得允许勾选该工厂单或点击出库按钮；只有订单数据库中的材料或五金事实发生变化，才提示用户可以更新出库信息。
- 已完成：出库入口和后端均按工厂单号、`outbound_status` 和当前数据库事实指纹执行重复出库保护；数据库事实变化后标记为“需要更新”，用户确认后更新原出库单，并同步库存记录、订单索引 SQLite 的工厂单状态/单据号/更新时间。订单中心不再读取或校验 Traveler。
- 影响范围：`macos/OrderDashboardView.swift` 的工厂单选择和出库按钮、`traveler_assistant/inventory.py` 的出库前预检、`tools/jdy_inventory.mjs` 的历史单查找/新增分支，以及相关业务规则和回归测试。
- 已验证：fixture 覆盖已出库阻断、数据库订单事实变化后进入更新、数据库来源预检无需 Traveler；174 个 Python 测试、macOS UI 回归、构建、正式签名安装和安装版启动检查通过。
- 未验证：本次未在真实库存系统中执行确认出库；真实库存系统原单更新和库存网页交互仍需用户现场验收。

## 已完成：清理出库预览区行数提示

- 用户要求：下一次修改出库界面其他内容时，同时删除材料预览标题下方的“2 行”数量提示。
- 已完成：删除材料预览标题下方的动态行数提示；预览明细、数量和确认前校验不变。
- 已完成：同步移除库存专用 Chrome 成功路径中已经不再显示的登录提示文案；失败提示仍保留。

## 已完成：精简订单中心出库入口提示并固定材料预览操作区

- 用户要求：订单中心选择工厂单后，出库入口旁不显示“已选 N 个工厂单”；仅保留“创建出库”按钮，工厂单多选逻辑不变。
- 用户补充的布局要求：材料预览显示区域单独滚动；出库操作按钮固定不滚动；整个出库页面不滚动。
- 相关实现位置：`macos/OrderDashboardView.swift` 的 `OrderDashboardFactoryPanel` 出库入口提示，以及 `macos/TravelerAssistant.swift` 的 `InventoryView` 预览列表和操作按钮布局。
- 已完成：移除“已选 N 个工厂单”提示；材料预览改为独立垂直滚动，操作按钮固定在预览区下方，订单上下文出库页面不再使用外层整页滚动；点击材料行任意位置可选中或取消选中，保留多选、忽略、恢复和设置映射功能。
- 已验证：完整发布测试、构建、签名安装和已安装 App 启动检查通过；未执行真实库存系统出库。

## 第 98 项：订单中心滚动范围调整

- 用户反馈：App 按当前默认窗口大小打开时，右侧出现覆盖整个订单中心页面的滚动条；希望右侧不再作为整页滚动容器。
- 待处理：仅让订单列表区域独立垂直滚动；顶部导航、搜索与筛选、操作按钮、最近结果区域和订单列表表头保持固定，不随订单列表滚动。
- 已完成：移除订单中心外层整页滚动，将垂直滚动限制在订单列表区域；顶部导航、搜索与筛选、操作按钮、最近结果区域和订单列表表头保持固定。
- 执行时需确认默认窗口尺寸下订单列表的可视区域、表头固定效果和滚动条位置；保留订单搜索、筛选、展开详情及其他业务行为不变。
- 已补充源码回归断言；发布门禁、构建、正式安装和已安装 App 检查在本次任务末尾执行并记录结果。
- 已验证：174 个 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版 App 启动检查通过；安装版订单中心仅保留订单列表滚动区域。

## 第 99 项：订单详情页布局与材料卡片调整

- 用户要求：订单详情显示区域调整为当前页面约 80% 宽度；下方淡蓝色信息卡调整为每行显示 4 个。
- 标题调整：将“订单详情”移到页面水平居中位置，并增大字体。
- 订单信息调整：订单号和订单状态在标题下方单独使用一个与板材卡片相同样式的显示区域，并在同一行显示。
- 材料卡片调整：板材、封边和五金继续使用统一的卡片视觉；每种商品显示为一个与板材相同大小和样式的卡片，只显示商品名称、数量和单位。
- 五金调整：按工厂单分别显示五金区域，每个工厂单一个显示区域；即使该工厂单没有五金数据，也保留一个五金显示卡的高度，避免页面布局跳动。
- 底部操作：将“生成 Traveler”和“关闭”两个按钮固定在页面下方并水平居中显示。
- 已完成：订单详情内容限制为页面约 80% 宽度，标题居中并放大；订单号和状态独立为同一行卡片；板材、封边和五金统一为四列卡片；五金按工厂单分区，无数据时保留占位卡片；底部按钮固定并居中。
- 执行时需保持订单级材料、工厂单级五金、数量和单位的现有数据含义不变；修改后需验证有数据和无五金数据两种详情状态，并按发布门禁重新测试、构建、正式安装和检查已安装 App。
- 已验证：174 个 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版 App 启动检查通过；未执行真实库存系统出库写入。

## 第 100 项：订单材料 SKU 入库门禁与五金忽略列表、订单详情布局待处理

- 用户确认的业务规则：忽略过一次的商品进入全局忽略列表；以后所有订单写入数据库前都自动比对该列表，命中的商品不写入有效材料/五金事实，也不进入出库。
- 待实现的入库门禁：订单板材、封边和五金写入中央 SQLite 前，必须与当前商品资料表匹配到有效 SKU，或已有有效人工映射，或用户明确选择全局忽略；存在未处理项目时，本次订单事实不得提交，订单进入待材料处理状态，处理后允许重新同步。
- 已确认的人工判断：`Penelope FA44` 封边映射 `M1043`；`Penelope FA44` 19.1mm 板材映射 `M1042`；`Woodline 3` 8mm 板材映射 `M1147`；`Walnut` 19.1mm 板材映射 `M0159`；`Walnut` 封边映射 `M0160`。这些映射待统一写入业务逻辑和映射数据，并补充商品类别、规格、启用状态校验。
- 已完成的数据清理：从中央数据库删除 `Square star basin-4-platform basin` 五金记录；后续实现入库门禁时需决定是否同时加入全局忽略列表，避免原始文件重新同步后再次出现。
- 待实现的设置页：重新设计五金忽略列表管理，支持查看现有项目、增加、删除和修改忽略项目及原因；修改后应影响所有订单的后续数据库写入，并保留可审计记录。
- 待实现的订单详情布局：顶部订单号和订单类型在同一行显示，订单号字体增大；详情内容出现滚动条时，板材区域和五金区域使用同一个滚动范围，能够同步滚动，不再让两块区域各自独立滚动。
- 执行边界：本项目前仅记录，尚未修改代码、映射清单或数据库业务逻辑；恢复执行时需补充迁移/历史数据审计、UI 回归、完整测试、构建、正式安装和安装版行为检查。

## 第 101 项：执行订单材料 SKU 门禁、全局忽略管理与详情页滚动调整

- 已完成：订单材料、封边和五金在写入中央 SQLite 前统一比对商品资料表；只有启用 SKU、有效人工映射或全局忽略可以通过。未完成项目创建待处理问题并阻止本次事实替换，避免先删旧数据后留下半成品。
- 已完成：WJ-CBD、WJ-CBT、WJ-YITONG 等来源编码保留为来源/显示追溯信息，不当作库存 SKU；已知来源显示名只用于后续商品匹配。
- 已完成：设置页新增五金全局忽略列表的查看、新增、修改和删除；忽略动作清理已有匹配五金事实，并影响所有后续订单。已写入确认映射：`Adjustable shelf holder`→`M1013`、`Cloth rod bracket - Fitting`→`M0179`、`Edge banding--Penelope FA44`→`M1043`、`19.1mm--Penelope FA44`→`M1042`、`8mm--Woodline 3`→`M1147`、`19.1mm--Walnut`→`M0159`、`Edge banding--Walnut`→`M0160`；`LED` 已加入全局忽略。
- 已完成：订单详情移除“订单详情”标题；订单号与订单类型同一行并放大订单号；内容区域水平居中、四列卡片保持一致高度；五金不显示单位；板材和五金共用一个垂直滚动区域。
- 已验证：最终发布门禁 179 项通过，macOS UI 回归通过，PP0067 工作簿端到端通过；最终版本已构建、正式签名安装到 `/Applications/PP FlowHub.app`，安装版设置页和订单详情页已检查。真实 Server 再次同步及库存系统实际出库仍未执行。

## 第 102 项：设置页只保留统一全局忽略列表

- 用户确认：设置页只需要一个总的忽略列表查看和修改入口，不需要按 Traveler 材料另设映射区域。
- 已完成：删除设置页中的 Traveler 材料名称输入、设置材料映射按钮和对应弹窗入口；保留库存资料更新和五金全局忽略列表的新增、查看、修改、删除。
- 保持不变：后台商品 SKU 匹配、人工映射数据和出库预览中的未映射处理逻辑继续保留，避免改变入库门禁和出库安全校验；本次仅收窄设置页展示范围。
- 已验证：179 项发布门禁、macOS UI 回归、PP0067 工作簿端到端通过；已重新构建、正式安装并重启安装版确认设置页只显示统一忽略列表，Traveler 材料映射区域不再显示。

## 第 103 项：补充全局忽略列表查看入口

- 用户反馈：设置页虽然显示了忽略内容，但没有明确的查看入口。
- 已完成：设置页显示忽略项目数量和“查看”按钮；点击后打开统一管理窗口，在同一处完成查看、新增、修改和删除。
- 已验证：179 项发布门禁、构建、正式安装和安装版界面检查通过；安装版可看到 LED、修改和删除按钮。

## 第 104 项：移除内置 LED 忽略并恢复材料映射管理入口

- 用户确认：忽略只从用户维护的全局忽略列表读取，不需要内置 LED 特殊忽略；当前列表中的 LED 已移除，后续是否忽略完全由列表决定。
- 已完成：删除 `WJ-CBD → LED` 的内置忽略别名；当前 `mappings.json` 忽略列表为空，历史 LED/WJ-CBD 数据库残留为 0 条。
- 已完成：设置页增加“材料映射”查看入口；独立管理界面显示现有 8 条映射，支持新增、修改和删除；五金忽略列表继续通过独立界面管理。
- 已验证：180 项发布门禁、构建、正式安装通过；安装版材料映射窗口显示 8 条映射，包含 `M1013` 和 `M0179`，忽略列表为 0 项且不再显示 LED。

## 第 105 项：映射和忽略规则迁移到中央数据库

- 用户确认：不要继续使用映射文件和忽略文件，所有规则统一保存到数据库。
- 已完成：中央 `workflow.sqlite3` 增加 `inventory_resolution_rules` 表，映射与忽略共用一套规则存储；同一个标准化材料名称不能同时存在映射和忽略规则。
- 已完成：库存预览、订单材料/五金入库、订单详情、出库预检查、设置页和 CLI 管理入口均改为读取中央数据库；显示使用保存的原始名称，不再把标准化匹配键当作界面名称。
- 已完成：旧 `mappings.json` 采用一次性事务迁移，迁移成功后移动到迁移归档目录，运行时不再读取；当前 8 条人工映射按已确认的可读名称迁移，忽略列表当前为 0 条。
- 已完成：迁移时校验映射 SKU 在商品资料表中存在且启用；保存新映射时继续执行同样校验。
- 已验证：180 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版设置页检查均通过；真实 Server/库存系统再次同步和实际出库仍未执行。

## 第 106 项：订单详情页固定顶部订单信息和操作区

- 用户要求：订单详情显示页面出现滚动条时，顶部订单号、订单状态和右上角操作按钮区域保持固定，不随下方内容滚动。
- 已完成：仅让板材、封边条和五金详情内容区域垂直滚动；订单号、状态、生成 Traveler 和关闭按钮保持在页面顶部可见。
- 已验证：198 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动检查通过。

## 第 107 项：统一五金来源选择、清理重复五金并移除标准订单 Traveler 前置依赖

- 用户确认：同一工厂单号在多个 Fittingslist 中出现时，按最新文件写入；订单详情、成本和库存只使用数据库事实，Traveler 仅在用户需要时按需生成。
- 已完成：新增共享 Fittingslist 选择器；数据库扫描和订单预览统一按文件修改时间选择最新工厂单，同一内容的重复文件自动去重，同一时间内容冲突时阻止静默选择并生成待处理问题。
- 已完成：清理中央数据库中的重复五金。PP0035 和 PP0063-2 的重复工厂单均已收敛；全库活动 AICNC 五金不再存在同一工厂单对应多个来源文件的记录。已确认 `Hinge`/`TestFullHinge` 映射 `M1001`，常见 H-Rail/L-Rail 使用既有规则。
- 已完成：标准订单库存预检改为直接读取中央 SQLite，不再查找或要求 Traveler；订单详情、成本、库存和数据库出库继续使用数据库事实。Traveler 生成/更新保留为显式按需操作；临时订单的独立出库兼容流程仍按其现有浏览器合同生成临时 Traveler。
- 已完成：旧映射迁移增加 SQLite `BEGIN IMMEDIATE`，避免多个后台任务并发初始化时重复插入迁移标记。
- 已验证：185 个 Python 测试、macOS UI 回归、PP0067 工作簿端到端和 diff 检查通过；正式 App 已构建、Apple Development 签名、安装到 `/Applications/PP FlowHub.app`，安装版启动检查通过后已退出。真实 Server 再次同步和库存系统实际出库仍未执行。

## 第 108 项：订单详情页重复刷新与滚动范围修复

- 用户反馈：打开订单详情时界面会反复刷新，滚动条没有只控制五金区域。
- 已完成：移除 `OrderDashboardDetailPage.onAppear` 对数据库详情的重复加载，保留订单打开时的单次加载；五金行改用工厂单号、商品编码、名称、规格和重复序号组成的稳定标识，不再每次生成新的 UUID。
- 已完成：订单号/按钮和板材封边区域固定显示，仅在五金区域建立独立垂直滚动容器。
- 已验证：185 个 Python 测试、macOS UI 回归、PP0067 工作簿端到端通过；正式 App 已重新构建、签名安装并完成安装版启动检查，验收实例已退出。

## 第 109 项：Server 常驻扫描服务（后续大版本候选）

- 用户提出：在 Server 所在主机部署开机自动启动的小程序，由它在本机每分钟扫描 Server 文件变化；PP FlowHub 只读取已经生成的扫描快照，避免通过 SMB 递归遍历目录。
- 当前决定：仅记录为后续大版本架构候选，本版本不实现、不安装 Server 程序，也不改变现有 SMB 扫描流程。
- 建议边界：Server 程序只提供路径、修改时间、大小、新增/修改/删除、扫描状态和时间等文件发现事实；AIMES、订单、出库和待处理规则继续由 PP FlowHub 中央 SQLite 负责。
- 建议通讯：优先在专用共享目录原子发布带版本号的压缩 JSON 快照和健康状态，不让 App 通过 SMB 直接读取正在写入的 SQLite；App 必须检查快照新鲜度和完整性，并保留本机扫描故障回退。
- 实施前置：确认 Server 操作系统、管理员安装权限、本地数据路径、开机服务方式、服务器负载和升级维护责任；先做只读原型并验证重启、服务中断、文件新增/修改/删除和快照损坏恢复。

## 第 110 项：Server 扫描方式统计显示

- 用户确认：订单中心 Server 扫描详情需要显示快速检查文件数、复用文件夹数、深度扫描文件夹数和总用时，便于核对扫描优化是否真正生效。
- 当前实现口径：相关 Excel 只读取路径、修改时间和大小，计为“快速检查”；当前扫描器仍递归枚举每个候选订单文件夹，所以“复用文件夹”为 0，“深度扫描文件夹”等于本次候选文件夹数。后续实现文件夹快照复用时沿用同一字段和界面文案。
- 已完成：`scan-server` 返回 `quick_checked_file_count`、`reused_folder_count` 和 `deep_scanned_folder_count`，操作详情显示快速检查文件数、复用文件夹数、深度扫描文件夹数、扫描范围、变化数量和总用时；后端与 SwiftUI 回归测试覆盖同一文案合同。
- 已验证：187 项 Python 回归、macOS UI 回归、PP0067 工作簿端到端和差异检查通过；正式 App 已构建、Apple Development 签名并安装到 `/Applications/PP FlowHub.app`。安装版启动后真实 Server 扫描和索引更新均完成，验收实例已退出；本次只扫描和更新本地索引，没有处理 Server 文件夹或执行库存出库。

## 第 111 项：本机数据库备份路径、分级保留和启动任务并发问题（已实现）

- 用户反馈：启动时提示需要备份数据库，点击备份后订单中心列表为空；已确认数据库本身仍有订单，实际问题包括备份 Server 路径不可用，以及启动时备份状态检查占用订单后台任务导致 `list-index` 被跳过且没有重试。
- 用户要求：数据库备份改到本机项目目录下；每天备份一次；最近三天每天保留一份；更早但属于最近一个月的备份每周保留一份；其他备份删除。已实现为独立的 `Config.database_backup_root`，不再复用 Traveler 文件备份目录。
- 实际路径：`/Users/lantian/Documents/pp-flowhub/data/database-backups`。该路径位于项目运行数据目录内，不使用 Server 或 iCloud。
- 已确认保留口径：以本地日期为准，保留今天及前两个自然日的每日备份；App 一整天没有打开、当天没有备份时不补造文件。再往前至最近 30 个自然日，每个自然周保留一份，优先保留该周日期最新的一份；超过 30 个自然日的备份删除。用户已将“最近五天”修改为“最近三天”，并接受滚动 30 天计算。
- 已实现每日触发：App 每个自然日第一次启动时自动尝试一次；先完成本地 `list-index` 读取，再在订单后台任务空闲时执行备份。当天已有成功记录时不重复生成；本机备份失败时显示明确错误并允许手动重试。设置页已增加“立即备份”入口。
- 建议启动并发修复：订单中心本地缓存读取必须优先执行，不能因 `backup-status` 或其他订单任务正在运行而静默返回；后台任务应排队或在 `orderRunning` 结束后重试，并保证失败时清除启动标记、更新错误状态。备份任务不应阻塞订单列表首次显示。
- 建议数据与审计处理：继续使用 SQLite Online Backup API 生成一致性快照；备份文件写入临时文件后原子改名，成功后再写入 `backup_records`。删除旧备份文件时保留成功记录作为审计历史，不能让界面把“记录存在”误认为备份文件仍可恢复；正式实施前需确定是否增加“文件已被保留策略删除”的状态字段。
- 已实现：使用 SQLite Online Backup API 写入临时文件后原子改名，成功后写入 `backup_records`；保留策略按今天/前两天每日、滚动 30 天内每个自然周最新文件执行。备份失败不写成功记录，并继续启动后的订单同步流程。
- 待本轮门禁验证：完整测试、重新构建、正式签名安装和安装版启动检查。

## 第 112 项：PP0065 重复材料事实清理（已完成数据修正，代码问题待处理）

- 已核对 Server 原始文件：`/Volumes/server/Optimized Orders/pp0065/pp0065 materials.xlsx` 解析出 6 条材料事实：18mm Plywood 4、14.5mm Plywood 3、5.4mm Plywood 1、Rosales 3 19.1mm Panel 17、Rosales 3 8mm Panel 9、Rosales 3 封边 239.56m。Server `Report/pp-板材清单-new*.xlsx` 同时显示板材数量一致，但封边汇总另有 266.9m 和 Finished 2.86m，不能把该报表封边数字直接替换 `material.xlsx` 的材料事实。
- 已确认重复原因：`workflow.sqlite3.material_items` 在同一订单下同时存在旧下载路径 `/Users/lantian/Downloads/server/Optimized Orders/pp0065/pp0065 materials.xlsx` 的 6 行和当前 Server 路径 `/Volumes/server/Optimized Orders/pp0065/pp0065 materials.xlsx` 的 6 行；两份材料文件 SHA-1 相同，6 组业务字段完全一致。当前同步删除条件包含 `order_id + source_path`，路径变化不会清理旧路径事实，因此形成重复。
- 已完成数据修正：先创建可恢复快照 `data/migration-archives/20260817-pp0065-material-dedupe-pre-fix/workflow.sqlite3`，再从中央数据库删除旧下载路径的 6 行；保留当前 Server 路径的 6 行。修正后 `material_items` 从 12 行收敛为 6 行，订单主表、工厂单和五金未修改；`PRAGMA integrity_check` 为 `ok`。
- 五金结论：Server 与旧下载目录的 Fittingslist 文件 SHA-1 相同，原始文件解析出 18 个项目；数据库当前有 15 行，全部来自旧下载目录路径，未发现同一数据库事实的重复行，但存在旧来源路径和名称/聚合差异。由于部分项目可能受库存映射、显示别名或历史忽略规则影响，本次未按猜测重建五金数据，待单独确认后处理。
- 已完成代码修正：Server 材料入库按订单级材料源集合替换旧 `aihouse` 事实，不再仅按完整 `source_path` 删除；同时写入材料源内容指纹，并增加旧路径迁移回归测试。PP0065 既有数据修正保持不变。

## 第 113 项：扫描期间打开订单详情显示空白（已实现）

- 已确认：双击订单时详情窗口会先显示，但扫描期间详情读取被 `orderRunning` 排队，页面原先没有等待状态，因此空数组被显示为“暂无材料/五金”。
- 已完成：扫描期间详情页显示“正在扫描，扫描完成后自动读取详情”，保留后台完成后的自动重试；扫描结束后仍从中央 SQLite 读取订单事实，不重新读取 Server 文件。
- 已验证：macOS UI 回归、完整 Python 发布测试、构建、签名安装和安装版启动检查通过。

## 第 114 项：识别被改名的订单文件夹（已实现）

- 用户要求：订单文件夹后来改名时，系统应尽量识别为原来的文件夹，不能简单显示为“旧文件夹删除 + 新文件夹新增”。
- 已完成：扫描会对同一 Server 根目录、同一父目录下的新增/消失文件夹进行安全配对；只有订单身份和识别报表的相对路径、类型、修改标记、大小全部一致且候选唯一时，才输出“改名”变化。
- 已完成：同步索引时更新 `source_files`、订单/工厂单/临时订单来源路径、活动问题路径，并记录 `server_folder_renamed`；后续读取沿用原索引事实，不产生旧文件夹删除和新文件夹新增。
- 安全边界：无法唯一确认时保持现有新增/删除行为，不能仅凭相似名称自动合并；需要覆盖标准订单、临时文件夹、混单文件夹和报表路径变化。
- 已验证边界：歧义、报表集合变化和普通新增/删除仍不合并；需随本轮完整测试、构建、正式安装和启动验收一起复核。

## 第 115 项：出库范围弹窗重新打开时回显已保存决定（已完成）

- 用户反馈：在订单详情中保存“客户提供材料（不入库存）”和原因后，再次打开“设置出库范围”页面，不能确认是否读取了上次保存的内容；当前页面可能只保留 SwiftUI 界面实例内存中的状态，不能把它当作数据库持久化。
- 已完成：弹窗打开时按订单号读取中央 SQLite `outbound_scope_decisions`，回显板材与封边/五金范围、对应工厂单、出库决定和原因；已有记录优先于默认值，没有记录时才使用现有默认值。
- 已完成：五金范围按选定工厂单恢复；订单没有可用五金时继续保持五金选项禁用。
- 已完成：异步读取只在弹窗打开时加载一次，并在读取期间禁用表单，避免迟到结果覆盖用户输入；保存成功后关闭并重新打开可恢复同一组值。
- 已验证：覆盖客户提供材料、无历史记录默认值、五金按工厂单回显和无五金禁用边界。
- 数据边界：本项不改变订单材料事实，也不把客户提供材料写入库存；只修正出库决定的读取与回显，继续保持材料事实和出库决定分离。
- 已验证：198 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动检查通过。

## 第 116 项：删除不可访问的独立 Traveler 预检/出库页面（已完成）

- 已完成：删除不可访问的独立 Traveler 预检/出库页面 UI 分支。
- 已完成：删除该页面对应的隐藏导航入口和自动跳转逻辑；订单详情的数据库出库流程改为唯一出库页面入口。
- 必须保留：订单详情中的数据库出库流程继续可用；订单材料映射、忽略规则等仍被其他流程使用的后端功能不得删除。
- 已完成：保留订单详情中的数据库出库预检/确认流程，以及订单材料映射、忽略规则后端和设置页管理功能。
- 已验证：订单详情出库源码回归、映射/忽略相关测试、macOS UI 回归、198 项 Python 发布门禁、构建、正式签名安装和安装版启动检查通过。

## 第 117 项：订单出库预检状态和材料行语义修正（已完成）

- 已完成：订单出库预检的总任务步骤在实际完成后同步显示成功，不再保留误导用户的转动图标。
- 已完成：订单上下文的材料预览行改为只读核对，不再显示没有实际出库作用的选择框；出库范围继续由数据库中的出库决定统一控制。
- 已验证：订单数据库出库预览、客户提供材料、不出库决定和未映射阻断逻辑均通过发布测试与 macOS UI 回归。

## 第 118 项：确认出库页面布局微调（已完成）

- 已完成：隐藏确认出库预检顶部不再需要显示的“点击记录行即可整行选中；按住 Command 可多选”提示。
- 已完成：预检操作记录卡与下方本次出库预览卡统一使用可用宽度；确认出库页面的“取消”和“确认出库”按钮组居中显示，并将两个按钮间距收窄为 8pt。
- 已验证：198 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动/退出检查通过。

## 第 119 项：预检页面“确认出库”按钮未居中（已完成）

- 用户反馈：确认出库预检页面底部的“确认出库”按钮仍未显示在页面中央。
- 已确认原因：该按钮位于预检页面的 `InventoryActionGrid`，不是最终确认弹窗中的按钮。`InventoryActionGrid` 使用自适应 `LazyVGrid`，外层设置了 trailing 对齐，但网格单元内的按钮没有独立的居中布局约束；外层 frame 的 alignment 不会把网格单元中的按钮居中，因此按钮仍按单元默认位置显示。
- 已完成：将预检页面底部操作区从自适应网格改为独立居中的操作区，使“确认出库”按钮在预览卡可用宽度内水平居中；按钮禁用条件、最终确认弹窗和库存写入逻辑保持不变。
- 已验证：199 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动/退出检查通过。

## 第 120 项：关闭出库页面后主订单列表未刷新（已完成）

- 用户反馈：CS002 已完成出库，关闭订单出库页面后，主界面的订单列表没有立即更新为已出库/已出货状态。
- 已确认原因：出库确认成功后，数据库中的工厂单出库事实已经写入；但订单列表当前使用的是打开出库页面前的 `dashboardOrders` 内存快照，关闭 `InventoryView` 的回调目前只关闭页面，没有重新读取 `list-index` 并应用新的订单汇总结果。订单派生阶段因此仍暂时显示旧状态。
- 已完成：关闭出库页面后执行一次轻量 `list-index` 刷新，重新读取本地数据库汇总并更新 `dashboardOrders`；不重新扫描 Server、不重复写出库数据，并保持当前展开订单和工厂单选择状态。若订单后台任务正忙，刷新请求会排队到当前任务结束后执行。
- 已验证：覆盖出库列表刷新代码合同、既有客户提供材料和真实库存出库路径的回归测试；199 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动/退出检查通过。

## 第 121 项：订单详情 Panel 颜色过多时换行（已完成）

- 用户要求：Panel 颜色超过一排可用宽度时允许显示为两排，但“Panel颜色”标签必须保持横向显示，不能被压缩成竖排。
- 已完成：颜色文本最多显示两行并按可用宽度换行；“Panel颜色”标签使用固定水平尺寸，保持单行横向显示；没有颜色时仍显示“—”。
- 已验证：Panel 颜色去重逻辑、布局源码合同、199 项 Python 发布门禁、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版启动/退出检查通过。

## 第 122 项：自有订单不支持选择出库范围（已完成）

- 用户确认：自有订单不提供“设置出库范围”能力；自有订单不能选择“客户提供材料”“余料生产”或“其他原因，不出库”等出库范围决定。
- 实施边界：订单类型为 `owned` 时隐藏或禁用订单详情中的“设置出库范围”入口，并在后端继续拒绝不符合业务规则的自有订单范围写入；自有订单的默认出库规则保持为已有材料/五金按正常出库处理。
- 非目标：不改变订单文件读取的材料/五金事实，不改变来料加工订单的出库范围设置，不改变工厂单选择和正常出库流程。
- 已完成：订单类型为 `owned` 时隐藏订单详情中的“设置出库范围”入口；后端拒绝自有订单的所有出库范围写入，保留已有材料/五金默认正常出库规则。
- 已验证：自有订单范围写入拒绝、来料加工订单范围流程、订单详情源码回归、200 项 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版进程/签名检查通过。

## 第 123 项：CS004 Traveler 的 Purchase List 页面未使用数据库事实（已完成）

- 用户反馈：打开 CS004 生成的 Traveler，在 `Purchase List订货单` 页面看到顶部有一个 `0`，且“工厂单名称”为空。
- 已确认：CS004 中央数据库存在活跃工厂单 `F2607310219`，工厂单名称为 `CS004-KITCHEN`；材料事实为 Woodline 4 Panel 4 张、Woodline 4 封边 102.42 米；当前五金事实为 0 条，数据库完整性检查为 `ok`。
- 已确认原因：`Purchase List!A4` 的模板公式引用空白的 `WorkOrderTraveler!A4`，因此 Excel 将空白引用显示为 `0`；当前数据库 Traveler 生成流程只更新 WorkOrderTraveler、Usage List 和 Picking List，没有更新 Purchase List 的工厂单名称、材料和五金内容。
- 已确认影响：Purchase List 中的 `19.1mm--`、`Edge banding`、`TB18`、`Poket Door Pantry Hinge`、`Touch Open` 等内容属于旧模板残留，不能作为 CS004 当前材料/五金事实；实际工厂单名称已写入生成 Traveler 的 Picking List。
- 后续修复范围：决定 Purchase List 是否继续保留后，使其与中央 SQLite 事实一致；修复空白引用产生的 `0`，正确显示工厂单号/名称、材料数量和实际五金；没有数据库五金时不得显示模板残留五金。
- 已完成：生成和更新 Traveler 时清空并重建 Purchase List；材料、工厂单名称和五金来自当前预览/中央 SQLite 事实，空白引用不再显示 `0`，没有数据库五金时不再保留模板五金。
- 已验证：真实 CS004 临时 Traveler 显示 `CS004-KITCHEN`，材料显示 Woodline 4 Panel 4 张和封边 102.42 米，未显示模板五金；200 项 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、正式签名安装和安装版进程/签名检查通过。
- 验收限制：安装版已成功启动并检测到进程，随后已退出；系统未授予 Codex/osascript Accessibility 权限，无法读取窗口控件进行自动化 UI 细节验收。

## 第 124 项：Server 材料分配完成后的界面关闭、刷新与排序（已完成）

- 用户要求：材料全部分配并写入订单后，写入确认界面自动关闭；主订单列表和待处理中心同步刷新；待分配明细按板材在前、封边在后，板材内先 Plywood 后 Panel；选择明细后自动回填当前剩余数量。
- 已确认原因：材料写入成功后原逻辑只在预览中的所有工厂单都移除时关闭，因此材料已写入但仍有工厂单预览时会停留在“材料已写入订单”；订单列表和待处理中心也保留写入前的内存快照。
- 已完成：材料写入成功后关闭当前材料分配弹窗；先刷新 `list-index` 更新主订单列表，再执行不自动弹窗的 `scan-server` 更新待处理中心。仍需确认的工厂单继续保留待处理状态，不被材料写入误标为完成。
- 已完成：待分配明细使用 Plywood、Panel、封边排序；选择明细或首次进入时将剩余数量回填到“本次分配数量”，并统一识别 `Edge banding` 等封边类型。
- 已验证：210 项 Python 回归、macOS UI 回归（含新增排序和数量回填规则）、PP0067 工作簿端到端、未签名构建、Apple Development 签名安装、安装版启动/退出检查通过。

## 第 125 项：Server material 按 Room/section 工厂单名称直接归属订单（已完成）

- 用户调整方案：不在 App 中把一条 material 明细手工拆分到多个订单；读取 material 文件发现混单、其他订单或无法判断归属时，要求用户直接修正源文件。
- 已完成：严格模式下从 `Room/section` 的工厂单名称提取订单前缀，例如 `PP0035-KITCHEN` 归属 `PP0035`、`PP0035-2-MASTER` 归属 `PP0035-2`，不再通过工厂单号或 AIMES 查询决定材料订单归属。
- 已完成：同一材料文件涉及多个订单时，缺少订单前缀、包含多个订单号、订单号不在当前文件夹或没有有效房间明细，均写入待处理问题并阻止该文件的材料事实写入；用户需手工修正 `Room/section` 后重新扫描。
- 已完成：删除 App 端材料分配弹窗、数量输入和对应 CLI 命令入口；材料在临时预览阶段按源文件订单归属，正式写入仍需用户确认订单/工厂单。
- 已完成：材料展示保持板材在前、封边在后，板材内先 Plywood 后 Panel；修复预览同步时映射读取与 SQLite 写事务冲突导致的 `database is locked`。
- 兼容边界：历史 SQLite 中的 `server_material_allocations` 表和后端兼容函数暂不删除，避免已有预览/数据库迁移受破坏；新的 App 流程不再调用它们。
- 已验证：订单工作流与订单索引相关测试 103 项通过；完整发布门禁 211 项 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、Apple Development 签名安装及安装版启动/退出检查均通过。

## 第 126 项：Server 扫描先校验材料，确认写入前保留订单/工厂单预览（已完成）

- 用户确认的流程：扫描 Server 时先检查 material；发现问题就提示用户手工修改源 Excel，重新扫描；材料校验通过后，订单、工厂单、五金等正式事实仍先进入预览，用户确认后才写入中央数据库。
- 已完成：扫描阶段对新增或修改的 material 使用隔离预览数据库进行校验，不写入生产订单/工厂单事实；失败时写入 `material_validation` 待处理问题。
- 已完成：材料问题存在时，点击“预览并逐单确认”不会打开写入确认窗口；修正并重新扫描后，问题会被关闭，才允许进入预览。
- 已完成：确认预览页文案改为“核对预览内容后确认工厂单”，不再提示用户先分配材料。
- 已验证：订单工作流与订单索引相关测试 104 项通过；完整发布门禁 212 项 Python 测试、macOS UI 回归、PP0067 工作簿端到端、构建、Apple Development 签名安装及安装版启动/退出检查均通过。

## 第 127 项：整单已出库后关闭陈旧的 Server 待处理问题（已完成）

- 用户反馈：PP0067 已通过 QTCK20260820003 出库，但待处理中心仍显示订单处理问题。
- 已确认原因：PP0067 的 `hardware_selection` 问题仍为 open；此前自动清理只覆盖 `material_validation`，且文件夹级问题路径没有被识别为订单文件夹。
- 已完成：当标准或混合 Server 文件夹中的全部订单均已出库时，自动关闭其中陈旧的材料校验和五金选择问题；保留 `sync_changes` 历史记录，不删除审计数据。
- 已完成：当前数据库中的 PP0067 陈旧问题已标记为 resolved，出库事实和单据 QTCK20260820003 未修改。
- 已验证：新增整单已出库后关闭五金选择问题的回归测试；完整发布门禁、构建、正式安装和安装版启动检查待本次交付完成后更新。
