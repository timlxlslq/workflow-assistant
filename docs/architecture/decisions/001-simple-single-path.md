# ADR-001：只保留单一现行路径

## 决定

项目不保留旧 UI、旧 CLI、旧工作表名或状态数据迁移逻辑。结构变化时直接更新现行代码、测试和文档。

## 理由

当前应用只有一个主要用户，且正在快速确定业务流程。兼容分支会增加测试面、路由歧义和维护成本，但不产生当前价值。

## 影响

- 开发中数据库 schema 变化可直接重建本地数据库。
- 只接受标准 Traveler 工作表名。
- 旧安装或旧状态不会自动迁移。
